/**
 * 승리에 필요한 점령 점수. 턴당 최대 1점이므로 곧 "점령 우위를 유지해야 하는 최소 턴 수"다.
 *
 * 10에서 낮춘 이유: 점령 조건을 인원 차 기준(CAPTURE_MARGIN)으로 바꾸면서 점수가 나는 턴이
 * 줄어 판 길이가 17턴대에서 30턴 가까이 늘었다. 승점 문턱을 같이 낮추지 않으면 규칙 하나를
 * 고친 대가를 판 길이로 치르게 된다.
 *
 * 판 길이가 승점에 비례해 줄지는 않는다 — 점령지까지 걸어가는 **고정 비용**이 있어서 10→7은
 * 29.8턴을 24.2턴으로만 줄였다(-30%에 -19%).
 *
 * 5까지 낮추면 판 속도는 19.6턴으로 거의 원상복구되지만 밸런스가 무너져 되돌렸다. 판이 짧을수록
 * 이동 3~4짜리 초반 템포 기물(돌진·기동형, 시간 역행형)만 이득을 보고 나머지 8종이 전부 내려가,
 * 전체편차가 13.7%p에서 15.3%p로 벌어졌다(충전 사격형은 41.8%로 측정 이래 최저). 7이 한동안
 * 잰 조합 중 편차가 가장 작았다.
 *
 * **7 → 15로 다시 올린 이유(사용자 결정)**: 평균 판당 턴수를 35~40턴 구간에 두고 싶다는
 * 요청. 이번 세션 측정(300판, 자유 편성·정원 맵·hard AI 미러전, 시드 고정이라 재실행해도
 * 값이 그대로다)으로 얻은 점수-턴수 관계는 7→24.0턴, 10→29.6턴, 14→35.2턴, **15→36.8턴**
 * (거의 선형: 1점당 약 +1.9턴)이었다. 15는 목표 구간 중앙에 걸리고, 전체편차도 14(19.0%p)보다
 * 15(18.4%p)가 근소하게 낮아 이 값을 택했다. 대가는 무승부율 증가(7 기준 ~7% → 15에서 9%,
 * `MAX_TURNS`=80 시뮬레이터 상한 안에 못 끝나는 판이 늘어난다 — 실제 게임엔 턴 제한이 없으므로
 * 측정치에만 해당). 기물 서열(측면 교란형 1위·방벽·제어형 판당사망 0.02 등)은 7일 때와 동일하게
 * 유지됐다.
 */
export const WIN_SCORE = 15;
/**
 * 사망한 기물이 시작지점에서 부활하기까지의 턴종료 틱 수.
 *
 * **2 → 3**(사용자 결정). 밸런스 측정만 보면 손해다 — 1000판 시드 짝지음에서 전체편차가
 * 14.8 → 16.5%p로 벌어졌고, 양쪽 이상치가 둘 다 잘못된 방향으로 움직였다(측면 교란형
 * 60.1 → 60.7%, 확률·포탑형 45.3 → 44.2%). 5턴 실험에서도 방향이 같았다(PROGRESS 20절).
 *
 * 그럼에도 3을 택한 이유는 시뮬레이터가 **AI 미러전만** 재기 때문이다. AI는 매 턴 새로 계획하고
 * 죽음을 두려워하지 않으므로 "죽으면 얼마나 쉬는가"의 체감 비용을 치르지 않는다. 사람이 느끼는
 * 판의 리듬은 이 숫자가 반박하지 못하는 축이라, 편차 1.7%p를 체감과 맞바꿨다.
 *
 * 이 값을 올릴 때는 `HEAL_PACK_RESPAWN_TURNS`도 함께 봐야 한다 — 아래 주석의 부등호가 깨진다.
 */
export const RESPAWN_TURNS = 3;
/** 최대 체력 = 체력 Lv × HP_MULTIPLIER (기획서 v0.3 6장) */
export const HP_MULTIPLIER = 5;
export const ROSTER_SIZE = 5;
/** dealer2 '시간 역행' 기술 id — 엔진(복귀 처리)과 UI(사용량 표시) 여러 곳에서 참조한다. */
export const REWIND_SKILL_ID = 'dealer2_rewind_move';

/**
 * 힐팩을 먹은 뒤 다시 찰 때까지의 턴 수. **`RESPAWN_TURNS`보다 길어야 한다** — 힐팩 재생성이
 * 부활보다 빠르면 "죽지 않고 힐팩만 순회하는" 운영이 언제나 옳아져 교전을 회피하는 판이 된다.
 *
 * 3 → 5: 부활이 2에서 3으로 오르며 두 값이 같아져 위 부등호가 깨졌다(길다 → 같다). 부활과 같은
 * 간격을 유지하려면 4면 충분하지만, 부등호가 아슬아슬하게만 성립하면 다음에 부활을 한 번 더
 * 올릴 때 또 깨진다. 5는 여유를 두고 끊은 값이다.
 *
 * 정원 맵에는 힐팩이 없어 기본 맵 밸런스 측정에는 잡히지 않는다 — 맵 메이커로 힐팩을 찍은
 * 맵에서만 실제로 작동하는 값이다.
 */
export const HEAL_PACK_RESPAWN_TURNS = 5;

/** 맵 메이커에서 찍을 수 있는 힐팩 등급(사용자 지정: 10힐 / 20힐). */
export const HEAL_PACK_AMOUNTS = [10, 20] as const;

/**
 * 점령에 필요한 **인원 차이**. 점령지 인원이 이만큼 앞서야 그 턴 1점을 얻는다.
 *
 * 1이면 단순 다수결이라 2:1도 점수가 난다. 2로 두면 2:1은 경합(0점)이고 3:1부터 점수가 나므로,
 * 한 명 더 밀어 넣는 것만으로는 점령이 안 되고 **확실한 우위**를 만들어야 한다. 곧 수비 한 명이
 * 공격 두 명을 묶어 두는 값을 갖게 되고, 점령지를 비운 채 교전만 하는 판이 줄어든다.
 *
 * 값을 더 올리면 원문 규칙("양 팀 공존이면 무조건 0점")의 교착 문제로 되돌아간다 —
 * 상세는 endOfTurn.ts의 점령 점수 주석 참고.
 */
export const CAPTURE_MARGIN = 2;
