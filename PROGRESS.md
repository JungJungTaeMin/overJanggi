# Simultaneous — 진행 상황

React + TypeScript + Vite로 구현한 동시 턴제 전략 점령 게임. 기획서(오버워치식 역할 분담 +
장기식 기물 운용 + 동시 턴 점령전) v0.3 전체 범위를 한 번에 구현했고, 이후 UI/UX 개선과
추가 기능 요청을 반영했다.

## 기술 스택 & 아키텍처

- **React 18 + TypeScript(strict) + Vite 5 + Zustand 4 + Vitest 2**
- 게임 로직은 프레임워크 비의존 순수 함수(`src/engine/*.ts`)로 작성. Zustand 스토어(`src/store/gameStore.ts`)는
  이 순수 엔진을 감싸는 얇은 래퍼.
- 대전 모드 3종: **로컬**(한 화면에서 양쪽 조작 — 원래의 디버그 모드), **AI**(휴리스틱 3난이도),
  **온라인**(PeerJS P2P, 방장 권위). GitHub Actions로 GitHub Pages 자동 배포.

```
src/
  data/            unitTypes.ts(10종 기물 실제 스탯), mapDefinitions.ts(정원 맵 7구역), constants.ts
  engine/           types.ts, resolveTurn.ts(5단계 오케스트레이터), resolvers/{movement,preAttack,attacks,healing,endOfTurn}.ts,
                     targeting.ts, grid.ts, rng.ts, validation.ts, death.ts, statusEffects.ts, moveReroute.ts,
                     capture.ts(점령 판정 — 엔진과 도움말이 함께 쓰는 규칙 원본)
  ai/               aiPlayer.ts(휴리스틱 계획 생성), difficulty.ts(난이도 프로필)
  online/           netBridge.ts(PeerJS 방 생성·참가, 방장 권위 스냅샷 동기화)
  store/gameStore.ts
  components/
    Menu/ModeMenu.tsx
    Board/{Board,UnitToken}.tsx
    Planning/{ActionPanel,UnitActionSelector,SkillTargetPicker,actionGeometry,planSummary}.ts(x)
    Hud/{Scoreboard,UnitStatusList,RespawnTracker,TurnControls}.tsx
    Log/ResolutionLog.tsx
    DraftSetup/{UnitPicker,PlacementScreen}.tsx
    Guide/{GuideOverlay.tsx, MiniBoard.tsx, rangeDiagram.ts}(게임 방법 — 사정권을 엔진 함수로 계산해 그림)
  test/{engine,ai,data,ui}/*.test.ts
```

## 핵심 게임 규칙 구현 현황

- **기물 10종** 전부 실제 스탯/스킬로 구현 (탱커 3종, 딜러 4종, 지원 3종 + support3 포탑 소환물).
- **포탑(support3 소환물)**: **회복 전용**(공격력 0·공격 불가, 주변 8칸 아군만 회복)이고 **이동 불가**이며
  **팀당 1기**(판 전체 최대 2기)다. 이미 포탑이 있는 팀이 새로 세우면 기존 포탑이 철거된다 —
  철거는 **이동보다 먼저**(0단계 `turnStart.ts`) 일어나야 자기 포탑에 스스로 길이 막히지 않는다.
- **정원 맵** 9×13 그리드, 7개 명명 구역(시작지점×2, 좌측 골목, 우측 다리/공터, 중앙 진입부, 점령지).
- **점령 점수(인원 차)**: 점령지 생존 기물을 팀별로 세어 상대보다 **`CAPTURE_MARGIN`(2)명 이상** 많은
  쪽이 1점(상대가 0명이면 1명만으로도 1점, 포탑 제외). 인원 차이 크기 자체는 무관하고 **턴당 최대
  1점**이라, 승리하려면 최소 `WIN_SCORE`(15)턴은 점령 우위를 유지해야 한다. 15점 선도달 즉시 승리 —
  7 → 15는 "평균 판당 턴수를 35~40턴 구간에 두고 싶다"는 요청으로 다시 올린 값이다(18절 참고).
- **턴 해결 5단계**: 이동 → 공격 전 상태변화(방벽/구속/공격모드) → 공격 → (dealer2 시간역행 복귀) → 회복 → 턴종료(쿨타임/상태이상/부활/점수).
  단계 순서는 항상 고정. 각 단계 내 처리 순서는 **우선순위**(이동 Lv desc → 공격력 desc →
  역할(딜러>탱커>지원) → 동률 시 무작위, `engine/priority.ts`)로 정하며, v0.5부터는 이동 단계 직전과
  공격 단계 직전(공격 전 상태변화 이후)에 각각 독립적으로 다시 계산한다(§3.2.1 — 두 단계 사이에
  죽거나 상태가 바뀐 기물이 반영되도록). 우선순위가 높은 기물부터 한 번에 하나씩 완전히 행동을
  마친 뒤 다음 기물이 진행된다(구 `firstMover` 코인플립 모델은 폐기).
- **보호막 체력**: 최대체력에 포함되지 않는 별도 체력(`shieldHp`)으로, 일반 체력보다 먼저 소모되고
  근원 효과가 만료되면 잔량과 무관하게 전부 사라진다(tank1 방어태세가 부여).
- **편성 규칙**(`data/rosterRules.ts`): 자유 편성(기본) / 탱커 1명 고정 두 가지를 시작 화면에서 고른다.
  스탯은 건드리지 않고 **드래프트에서 무엇을 고를 수 있는가**만 바꾼다. 드래프트 화면·AI 편성·밸런스
  시뮬레이터가 모두 이 파일 하나를 근거로 삼는다.
- 스테일메이트 규칙은 기획서 원문 그대로 미정 — `resolvers/endOfTurn.ts`에 TODO 주석만 남기고 미구현.

## 검증

- `npx tsc --noEmit`, `npm test`(Vitest 153개 테스트, 전부 통과), `npm run build` 모두 통과 상태 유지.
- 골든패스 통합 테스트(`test/engine/goldenPath.test.ts`): 실제 정원 맵 + 실제 5기물 로스터로 배치 →
  다턴 진행 → 강제 점령 → 15점 승리(`phase === 'gameOver'`)까지 검증.
- 신규 기능은 `mcp__Claude_Preview` 로 실제 개발 서버를 띄워 브라우저에서 직접 클릭/키 입력을 재현해
  렌더링 결과와 스토어 상태를 함께 확인(코드 리뷰뿐 아니라 실동작 검증).

## 최근 작업 이력

### 1) 최초 검증 (이전 세션에서 구현된 코드를 처음으로 컴파일/테스트)
- 프로젝트가 한 번도 `tsc`/`vitest`/`build`를 실행해본 적 없는 상태였음 — 전부 실행해 첫 실전 검증 수행.
- `resolveTurn.ts`의 TS2367(불가능한 비교로 오판된 타입 좁히기) 실버그 수정: `state.phase`를
  `as GameState['phase']`로 명시적으로 넓혀서 `gameOver` 여부를 재비교하도록 수정
  (단순 타입 애노테이션으로는 해결되지 않고, `as` 단언이 필요했던 TS 특이 케이스).
- 골든패스 통합 테스트 신규 작성(실제 맵 + 실제 로스터 조합이 그동안 한 번도 같이 테스트된 적 없었음).

### 2) UI/UX 전면 개선 ("ui ux가 너무 불편해")
문제 진단: (a) 보드 클릭으로 행동을 지정할 수 있는 인프라가 있었지만 실제로 연결되어 있지 않았음,
(b) 10기물의 편집기가 항상 전부 펼쳐져 있어 화면이 매우 복잡했음.

- **보드 클릭 ↔ 드롭다운 동기화**: `components/Planning/actionGeometry.ts` 신규 — 선택된 유닛의
  이동/공격 가능 칸을 계산해 보드에 하이라이트(초록=이동, 주황=공격)하고, 클릭하면 드롭다운과 동일한
  액션이 지정되도록 구현. 두 입력 방식이 완전히 호환됨.
- **접이식 기물 카드**: `ActionPanel.tsx` 재작성 — 선택된 유닛 1개만 상세 편집기(`UnitActionSelector`)가
  펼쳐지고, 나머지는 이름/HP바/상태 배지/행동 요약 한 줄(`planSummary.ts`)로 축약.
- **디자인 시스템**: `src/index.css` 신규(CSS 커스텀 프로퍼티 기반), 전 컴포넌트의 인라인 스타일을
  클래스 기반으로 교체.
- 브라우저에서 실제 클릭 이벤트를 재현해(보드 클릭 이동/공격, 행동 초기화 버튼 등) 정상 동작을 확인.

### 3) 키보드 단축키
선택된 유닛에 대해 `m`=이동, `a`=공격(공격 불가 기물은 무시), `s`=기술1, `u`=기술2(없으면 무시),
`h`=힐 계열 기술(없으면 무시)을 매핑. 입력창 포커스/모디파이어 키/기획 단계가 아닐 때는 무시하도록 가드.
tank3(스킬 2개)·support1(힐 스킬)·dealer1(스킬 0개) 세 가지 케이스로 실제 브라우저에서 검증.

### 4) 최신 3종 기능 요청 반영
1. **공개 전 "이동 예정" 미리보기(고스트)**: 실제 유닛 상태(`state.units[].position`)는 "공개 및 해결"을
   누르기 전까지 그대로 두고, 계획된 이동의 도착 칸에 반투명·점선 테두리 고스트 토큰과 원위치→도착지
   점선 화살표를 겹쳐서 보여준다(`Board.tsx`, `UnitToken.tsx`에 `ghost`/`previewPosition` prop 추가,
   `actionGeometry.ts`의 `previewMoveDestination()`으로 목적지 계산). 실제 이동은 여전히 해결(reveal) 시점에만 일어남.
2. **딜러2(시간 역행형) 추가 이동 시 경로 변경**: 기존에 tank2_charge(돌진)에만 있던 "경로 변경" 로직을
   `engine/moveReroute.ts`로 공용화하고 dealer2_rewind_move에도 적용. 기본 이동속도(3칸)까지는 원래
   방향으로, 충전 기술로 얻은 보너스 칸부터는 다른 방향으로 꺾을 수 있음. 엔진(`resolvers/movement.ts`)과
   미리보기(고스트)가 같은 로직을 공유해 항상 일치. 신규 Vitest 2건(방향 전환 있음/없음) 추가·통과.
3. **chess.com 스타일 사전 하이라이트**: 유닛을 선택하면 현재 행동 종류(이동/공격 중 뭘 선택했는지)와
   무관하게 이동 가능 칸(초록)과 공격 가능 칸(주황)을 항상 동시에 표시(`App.tsx`의 `moveOptions`/`attackOptions`
   계산 조건 변경). 겹치는 칸은 클릭 시 이동이 우선되도록 하이라이트 색상 우선순위도 클릭 로직과 일치시킴.

세 기능 모두 `tsc`/`vitest`/`build` 통과 후, 실제 개발 서버(브라우저)에서 드래프트→배치→계획→해결까지
직접 실행해 다음을 확인:
- 해결 전 실제 좌표는 그대로이고 고스트만 목적지에 표시됨.
- 해결 후 실제 좌표가 고스트가 보여준 위치(굽은 경로 포함)와 정확히 일치.
- 유닛 선택 시 이동/공격 칸이 즉시 동시에 표시됨.

### 5) 기획서 v0.3 전면 재검토 및 엔진 재작성

사용자가 v0.3 기획서(상태: "핵심 규칙 확정, 스테일메이트·보호막 세부 계산 규칙 미정") 전문을 다시
공유. 기존 코드와 원문을 항목별로 대조해 6가지 괴리를 확인하고("A" 응답으로 전체 반영 확정):

1. **체력 공식**: `maxHp = hpLv × 2` → `maxHp = hpLv × 5`(6장). `data/constants.ts`의
   `HP_MULTIPLIER` 값만 바꾸면 전체에 전파되도록 이미 상수화되어 있었음.
2. **턴 우선순위 모델 전면 교체**: "동순위는 무작위 firstMover가 우선"이라는 단순 규칙 대신,
   §3.2.1이 명시하는 "이동 Lv desc → 공격력 asc → 역할 → 동률 시 무작위" 계산식을 턴 시작 시 1회
   실행해 이동·공격 단계 전체에서 공용으로 쓰는 모델로 교체. 신규 `engine/priority.ts`
   (`computeTurnPriority`)가 이 계산을 전담하고, `resolveTurn.ts`가 결과를 `state.lastPriorityOrder`에
   저장 + `'priority'` 단계 로그로 기록.
3. **해결 모델을 "동시 스텝 시뮬레이션+배치 적용"에서 "우선순위 순차·즉시 적용"으로 전환**:
   `resolvers/movement.ts`·`resolvers/attacks.ts`를 함께 재작성 — 이동·공격 모두 우선순위 순서대로
   기물 하나씩 행동을 완전히 마친 뒤 다음 기물로 넘어간다. 신규 공용 헬퍼 `engine/damage.ts`
   (`applyDamage`, 보호막 우선 소모)를 도입해 피해를 즉시 적용하고 즉시 `killUnit`을 호출하도록 통일.
   부작용: 기존에 "상호킬은 항상 양쪽 다 사망"이던 처리가 "우선순위가 높은 쪽이 먼저 죽이면 상대는
   반격하지 못하고 일방적으로 패배"로 바뀜(3.6절 "사망한 기물은 해당 턴 이후 행동을 하지 않는다"와
   합치).
4. **보호막(shieldHp) 신규 도입**: tank1 방어태세가 더 이상 `maxHp`/`currentHp`를 직접 올리지 않고,
   최대체력과 별도인 `shieldHp`를 부여(8장). `applyDamage`가 일반 체력보다 먼저 소모하고, 근원 효과가
   만료되면(`resolvers/endOfTurn.ts`) 잔량과 무관하게 전부 사라진다. **판단 필요 항목**(기획서 §10
   미정, 기본값 채택): 부여량 = `hpBonus × HP_MULTIPLIER`, 방벽과 동시 적용 시 방벽이 먼저 막아
   보호막/체력 모두 그대로 두는 것을 기본값으로 채택(코드 구조상 자연히 그렇게 동작).
5. **방벽 규칙 반전**: "1회 피격 시 소멸" → "지속시간(1턴) 끝까지 유지, 자연 만료로만 제거"로 변경(8장
   재검토). `consumeBarrier` 헬퍼 삭제, `attacks.ts`가 더 이상 호출하지 않음. 또한 범위형(AoE) 공격은
   방벽을 아예 무시하고 관통하도록 변경(직선 단일대상 공격만 방벽에 막힘).
6. **부활 규칙 보강**: 기술2(2번째 스킬, 예: tank3_root)의 쿨타임은 부활해도 초기화하지 않도록
   `respawnUnit`을 수정(나머지 쿨타임/충전/상태이상/보호막은 기존처럼 전부 초기화). 시작지점이 전부
   점유되어 있으면 각 시작지점 칸의 인접 칸을 좌→우→상→하 순으로 탐색해 첫 빈 칸에 부활하는
   `findRespawnCell` 헬퍼 신규 추가(기획서 미명시, 판단 필요 항목 기본값).

**UI 반영**: `ResolutionLog.tsx`가 `'priority'` 단계 로그를 순위·근거가 포함된 목록으로 렌더링(9장
"우선순위 표시" 요구사항). `UnitToken.tsx`(보드 토큰 위 하늘색 보호막 바 + 툴팁), `UnitStatusList.tsx`,
`ActionPanel.tsx`(보호막 배지)에 `shieldHp` 표시 추가.

**테스트 갱신**: HP 배율 변경으로 죽지 않게 된 기존 어서션들(공격력 8 vs 새 maxHp 15인 support2 등)을
`currentHp`를 낮게 세팅해 사망을 강제하는 방식으로 보정. "상호킬" 테스트는 "우선순위 승자의 일방적
킬"로 재작성(`rngFor` 헬퍼로 동률 타이브레이크 고정). "방벽 1회 소멸" 테스트는 "방벽 유지" 테스트로,
"AoE가 방벽을 대상별로 개별 판정" 테스트는 "AoE는 방벽을 무시하고 관통" 테스트로 전면 수정.
`tsc --noEmit`/`npx vitest run`(25개 전부 통과)/`npm run build` 모두 재확인.

### 6) 기획서 v0.5 전면 재검토 및 재조정

사용자가 v0.5 기획서(상태: "핵심 규칙 확정, 스테일메이트 규칙만 미정") 전문을 다시 공유하고
"이거보고 ㄱㄱ"로 즉시 반영을 지시. v0.3 기준으로 구현했던 6가지 항목 중 일부가 v0.5에서 다시
바뀌었거나 더 구체화되어, 코드와 원문을 항목별로 재대조해 7가지를 수정:

1. **공격력 동률 타이브레이크 방향 반전**: §3.2.1이 "이동 Lv 동률 시 공격 수치가 **높은** 쪽이
   우선"이라고 명시함을 재확인 — v0.3 재검토 때 반대 방향(asc)으로 잘못 반영했던 것을 바로잡음
   (`engine/priority.ts`의 비교식을 `a.attack - b.attack` → `b.attack - a.attack`으로 수정). 신규
   결정론적 테스트(dealer1 vs tank1, 공격력만 다르고 이동 Lv는 동률) 추가.
2. **이동·공격 단계별 독립 우선순위 계산**: §3.2.1을 다시 읽어 "단계 직전에 각각 계산"으로 명확화 —
   턴 시작 시 1회만 계산해 두 단계에 공용으로 재사용하던 기존 모델을 폐기하고, `resolveTurn.ts`가
   `computeTurnPriority()`를 이동 단계 직전과 공격 단계 직전(공격 전 상태변화 이후) 두 번 독립
   호출하도록 재작성. `GameState.lastPriorityOrder`를 `{ movement, attack }` 형태로 분리하고
   `ResolutionLog.tsx`도 `'movementOrder'`/`'attackOrder'` 두 종류의 로그를 구분 렌더링하도록 갱신.
3. **보호막(shieldHp) + 최대체력 동시 부여로 복원**: v0.3 재검토 때 "체력 Lv 증가 = 보호막만 부여"로
   잘못 반영했던 것을, §6/§7.1/§8이 "최대체력 증가분과 동일한 양의 보호막을 **함께** 부여하고,
   지속시간이 끝나면 늘어난 최대체력과 남은 보호막을 함께 제거한다"고 명시함을 재확인해 원상 복구.
   신규 `'hpBuff'` 상태이상(증가량을 `magnitude`로 저장)을 도입해 `'shield'`와 나란히 부여하고,
   `resolvers/endOfTurn.ts`에서 두 효과가 동시에 만료될 때 `maxHp`를 되돌리고 `currentHp`를 새
   상한으로 clamp하는 로직을 `shieldHp` 초기화 로직 옆에 추가. tank1_fortify가 이 두 효과를 함께
   부여하도록 `resolvers/movement.ts` 수정.
4. **AoE 공격의 장애물/시야 차단**: §3.4/§8 "장애물이 놓인 칸은 범위형 공격의 대상이 아니다"를
   신규 반영 — `targeting.ts`에 브레젠험 기반 `hasLineOfSight(from, to, board)`를 추가하고
   `frontBandCells()`가 각 대상 칸을 `inBounds` + `!isObstacle` + 시야 확보 여부로 필터링하도록 수정
   (단, 방벽은 범위 공격의 시야를 차단하는 장애물로 취급하지 않음 — 기존 "AoE는 방벽을 무시하고
   관통한다" 규칙과 별개로 유지). tank3의 3칸 밴드 모양상 시야 차단이 실질적으로 발동하는 경우는
   거의 없지만(모두 사거리 1 이내), 장애물 자체가 대상 칸일 때 막히는 것이 실제 동작 변화 지점.
5. **사망 중 기술2 쿨타임 감소**: §8 "사망한 동안에도 기술 2의 쿨타임은 정상적으로 감소하지만,
   부활 시 초기화하지 않는다"를 신규 반영 — 기존에는 사망한 기물의 쿨타임 감소 루프 자체를
   건너뛰었으나, `resolvers/endOfTurn.ts`의 부활 카운트다운 루프 안에 기술2(스킬 목록의 2번째)
   전용 감소 처리를 별도로 추가(다른 쿨타임/충전/상태이상은 여전히 사망 시 전부 초기화되고
   감소 대상에서 제외됨).
6. **부활 위치 동시 충돌 해소 알고리즘 전면 재작성**: §3.5 "같은 턴종료 틱에 부활하는 여러 유닛이
   동일한 후보 칸을 동시에 제안하면, 부활 순서나 선공권을 적용하지 않고 무작위로 한 명만 확정하고
   나머지는 다음 후보로 넘어간다"를 반영해 기존의 "실패 시 첫 빈 인접 칸으로" 단일 유닛 전용
   `findRespawnCell`을 폐기하고, owner별 공유 후보 목록(`buildRespawnCandidates`) + 라운드 기반
   동시 제안/확정 루프(`respawnUnits`)로 재작성. **판단 필요 항목(기획서 미명시 세부사항)**: 문서가
   말하는 "무작위"를 이동/공격 우선순위(스탯 기반)와는 무관한 별도의 공정한 추첨으로 해석해, 이미
   `resolveTurn`에 주입되는 `rngFn`을 재사용하는 승자 추첨을 채택 — 완전히 동률인 두 유닛이 매
   라운드 같은 칸에서 계속 부딪히기만 하는 교착을 막기 위한 최소한의 장치. `resolveEndOfTurn`
   시그니처에 `rngFn` 파라미터 추가. 신규 테스트로 동시 사망한 두 유닛이 같은 틱에 부활할 때
   서로 다른 칸에 배정되고 절대 충돌하지 않음을 검증.
7. **사망으로 취소된 공격의 로그 이벤트**: §3.6/§9 "사망한 기물은 해당 턴의 이후 행동을 수행하지
   않는다"의 UI 표시 요구사항을 반영 — `resolvers/attacks.ts`의 우선순위 루프에서, 공격을
   계획했지만 자기 차례가 되기 전에(이동·공격전 단계에서, 혹은 같은 공격 단계 내 자신보다 먼저
   처리된 상대에게) 이미 사망한 기물에 대해 `{ phase: 'attack', type: 'cancelledByDeath', actorId }`
   로그를 남기도록 추가. (초기 구현은 이 검사를 공격 단계 진입 직후 한 번만 수행해 같은 단계 내
   사망은 놓치는 버그가 있었음 — 검사를 우선순위 루프 안, 각 기물 차례가 될 때로 이동해 수정.)

**부수 수정**: `unitTypes.ts` 헤더 주석을 방벽 지속/AoE 관통/AoE 장애물 차단, 보호막+최대체력
동시 부여·해제 규칙으로 갱신.

**테스트 갱신**: 신규 6건 추가(동률 방향 반전 1건, AoE 장애물 차단 1건, 사망 취소 로그 1건, 보호막+
최대체력 동시 부여/해제 1건, 사망 중 기술2 쿨타임 감소·부활 시 보존 1건, 동시 부활 충돌 비충돌
1건) — 전부 `test/engine/{movement,attacks,skills}.test.ts`에 위치. `tsc --noEmit`/
`npx vitest run`(31개 전부 통과)/`npm run build` 모두 재확인. 구현 중 `finalizeRespawn`에서 더 이상
쓰이지 않게 된 `state` 파라미터를 제거해 미사용 변수 타입 오류를 해결.

### 7) dealer2 시간 역행 UX 전면 구현 + 구간별 이동 경로("한 번의 이동 = 한 방향")

사용자 요청("남은 사용량 3/3 표시 … 실제로 기술을 통해 추가이동이 안되는 버그 고치고 / 추가이동을 할때
무조건 한방향이 아니라 방향전환 가능 / ui적으로는 1번이동 2번이동 …을 표시")에 따른 작업. 애매한 두
지점은 되묻고 답을 확정한 뒤 구현: **턴당 3회까지 사용 가능**, **복귀 시점은 턴종료가 아니라 공격
단계 직후**, **스텝별 방향 지정은 dealer2 전용이 아니라 이동 가능한 전 기물 공통**. 이후 사용자가
"충전 1회 = 1칸"이 아니라 **"충전 1회 = 이동을 한 번 더 = 이동 Lv(3칸) 통째로"**임을 확정해
최대 이동은 3(기본) + 3×3 = **12칸**이 되었다.

1. **"추가 이동이 안 된다" 버그의 진짜 원인**: 엔진은 이미 보너스 이동을 지원하고 있었고(테스트로 확인),
   UI의 `computeMoveOptions`가 `typeDef.moveSpeed`만 보고 이동 가능 칸을 계산해 **늘어난 칸을 아예
   초록으로 표시하지 않은 것**이 원인이었다. 신규 `moveCapacity(unit, turnNumber, skillUse)`가
   기본 이동력 + 지속 중인 `moveBonus` + **이번 턴 계획에 담긴 기술 보너스**를 모두 더하도록 하고
   `App.tsx`에서 이 값을 넘기게 해 해결.
2. **스텝별 경로 모델**: `BaseAction.move`에 `path?: Direction[]` 추가. 존재하면 이것이 경로의 유일한
   근거이고 `direction`/`distance`는 요약값(첫 스텝/길이)이 된다. 없으면 기존 "한 방향 × 칸수" 해석이
   그대로 동작하므로 완전한 하위 호환(기존 31개 테스트가 수정 없이 통과). 해석 로직은 신규
   `engine/movePath.ts` 한 곳에 모아 해결 엔진과 UI 미리보기가 절대 어긋나지 않게 했다
   (`moveReroute.ts`는 이제 구형 계획 확장 용도로만 남음).
3. **충전 N개 동시 사용 + 충전 1개 = 이동 한 번 더**: `SkillUse.amount?: number` 추가. dealer2는 한
   턴에 1~3개를 소모하고 **충전 1개가 이동 Lv(3칸)을 통째로** 더해 준다 → 1개 6칸, 2개 9칸, 3개 12칸.
   스킬 payload는 `{ extraMoveMultiple: 1 }`이고 실제 보너스는 `충전수 × moveSpeed × multiple`로
   계산하므로, 이동 Lv이 다른 기물이 같은 종류의 기술을 갖게 돼도 수치를 하드코딩할 필요가 없다.
   `validation.ts`가 실제 소모량 기준으로 이동 상한을 검사하고, 대각선 이동이 불가한 기물의 path에
   대각선이 섞이면 계획 전체를 무효화한다.
4. **복귀를 공격 직후로 이동**: 신규 `engine/resolvers/rewind.ts`를 만들고 `resolveTurn`이 공격
   단계 바로 뒤에 호출한다(`endOfTurn.ts`에서는 제거). 그 턴에 받은 피해까지 되돌리는 것이 기술의
   취지이므로 턴종료까지 미루면 안 된다. 기준점 칸이 막혀 있으면 위치 복원만 생략하고 체력·충전은
   정상 처리(기획서 미명시 — 겹침 금지 규칙 우선).
5. **기준점 생명주기**: 첫 사용 시점의 위치·체력을 자동 기록(`rewindAnchor` 로그), 충전 0 도달 시
   복귀 후 초기화, `death.ts`에서 사망 시 기준점 제거, 부활 시 충전 최대치 복구.
6. **추가 이동은 그 턴 한정**: 구현 후 브라우저 검증 중, dealer2의 추가 칸을 tank1/tank2처럼
   `moveBonus` 상태이상으로 주면 상태이상의 "1턴 동안"이 다음 턴까지 유효해(§statusEffects) 충전을
   쓰지 않은 다음 턴에도 추가 칸이 남는 문제를 발견 — 게다가 복귀 직후 충전이 3/3으로 초기화되므로
   기술을 쓰지 않은 다음 턴에도 공짜 추가 이동을 얻는다. 시간역행 보너스만 `movement.ts` 안의 턴
   지역 맵(`rewindExtraMove`)으로 계산하도록 변경(tank1/tank2는 기존 상태이상 방식을 유지).
7. **UI**: `UnitActionSelector`에 `1번 이동 / 2번 이동 …` 방향 선택 행(기본 이동력 초과 행은 보라색
   `시간 역행` 배지)과 `+ n번 이동 추가` 버튼, 그리고 시간 역행 패널(`남은 사용량 3/3 → 사용 후 0/3`,
   기준점 좌표·기준 체력, 사용량 선택, 충전 0 경고, `사용 / 취소 / 일반 이동으로 변경` 버튼).
   `Board.tsx`는 직선 한 줄 대신 **스텝별 선분**을 그리고 추가 이동 구간만 보라색 실선으로 구분하며,
   각 칸에 스텝 번호와 기준점 마름모(`⟲체력`)를 표시한다. `ActionPanel` 접힌 행과
   `UnitStatusList`에 남은 사용량·기준 체력을 노출하고, `ResolutionLog`는 복귀 전후 위치·체력을
   JSON이 아닌 한국어 문장으로 렌더링한다.
8. **구간(segment) 표시**: 충전 1회가 이동 한 번을 통째로 더해 주므로 "기본 이동으로 여기까지 /
   기술을 1회 더 쓰면 여기까지"를 볼 수 있어야 한다. `movePath.ts`에 `moveSegmentLength`(=이동 Lv)와
   `segmentIndexOfStep`을 두고, 경로를 이동 Lv 단위로 끊어 패널에는 `기본 이동` /
   `기술 n회 사용 — 추가 이동` 머리말과 `… 도착 (x, y)` 칩을, 보드에는 각 구간 도착 칸에
   `기본` / `기술1` / `기술2` / `기술3` 원형 표식을 그린다.
9. **구간은 서로 독립 — 길이도 방향도 제각각**: "기본 아래3 → 기술1 왼쪽2 → 기술2 아래3 →
   기술3 오른쪽3"처럼 **한 구간을 덜 쓰고 다음 구간으로 넘어갈 수 있어야** 한다는 사용자 예시에 따라,
   평평한 경로를 이동 Lv로 균등 분할하던 모델을 버리고 `BaseAction.move.segmentLengths`(예 `[3,2,3,3]`)를
   추가했다. 엔진의 실제 이동은 여전히 평평한 `path`만 보므로 해결 로직은 그대로고,
   `segmentLengths`는 "어디까지가 기본 이동이고 어디부터가 기술 n회째인지"를 보존한다.
   `validation.ts`는 총합뿐 아니라 **구간별 상한**도 검사한다 — 그렇지 않으면 `[3,0,0,9]`처럼
   앞 구간을 아껴 한 번의 이동에 9칸을 몰아쓰는 계획이 총합 12칸이라는 이유로 통과해 버린다
   (덜 쓴 칸은 이월되지 않고 그냥 버려지는 것이 규칙). UI도 구간별 블록으로 재구성해
   각 구간이 자기 `+ 칸 추가` 버튼과 `(2/3칸)` 사용량 표시를 갖는다.
10. **패널·보드 도착점 불일치 수정**: 검증 중, 패널의 도착 좌표는 단순 좌표 덧셈이라 장애물에 막혀
   실제로는 갈 수 없는 칸을 "도착"으로 표시했고, 보드 쪽은 잘린 마지막 스텝이 구간 경계가 아니면
   표식을 아예 그리지 않았다. `previewMoveSteps`가 잘린 마지막 스텝을 항상 도착점으로 표시하도록
   고치고, 패널도 같은 함수의 결과를 그대로 쓰도록 바꿔 두 곳이 구조적으로 어긋날 수 없게 했다
   (막힌 스텝에는 `막힘 — 여기까지 가지 못합니다` 표시 추가).
11. **"한 번의 이동은 무조건 한 방향"**: 앞선 4)-2와 7)-2에서 "이동 가능한 전 기물이 칸마다 자유롭게
   꺾을 수 있다"고 구현했던 것을 사용자가 뒤집었다 — 방향 전환이 가능한 경우는 **① dealer2가 추가
   이동을 써서 구간(기본 이동 → 기술 1회 → …)이 바뀌는 경계**와 **② tank2 돌진처럼 경로 변경을
   명시적으로 허용하는 기술**뿐이다. `movePath.ts`에 `inSegmentTurnAllowance`(기술 payload의
   `rerouteCount`, 기본 0)와 `countInSegmentTurns`(구간 경계가 아닌 곳에서 방향이 꺾인 횟수)를 추가하고
   `validation.ts`가 이를 초과하는 계획을 거부한다. dealer2의 구간 경계 전환은 구간 *내부*가 아니므로
   자연히 허용된다.
   **UI 재구성**: 이동 편집기가 "칸마다 방향 select"에서 **구간별 `방향 + 칸수` 한 쌍**으로 바뀌었다.
   경로 변경이 허용되는 기술을 쓸 때만 `+ 경로 변경 (돌진 — 1회 가능)` 버튼이 나타나 그 구간에
   `꺾어서` 라벨이 붙은 두 번째 직진을 추가한다. 이때 새 직진의 기본 방향은 **앞 직진과 실제로 꺾이는
   방향 중 그 지점에서 한 칸 갈 수 있는 것**을 고른다 — 경로가 평평한 방향 배열이라 같은 방향(또는
   반대 방향)을 넣으면 `runsOfSegment`가 도로 하나로 합쳐 버려 버튼을 눌러도 아무 일도 안 일어난다.
   tank2의 구형 `경로 변경(1칸 이동 후부터 적용)` select는 이제 `+ 경로 변경`과 완전히 중복이고
   `path`가 있는 계획에는 애초에 영향을 주지 못하므로 제거했다.
12. **초록 이동 칸도 구간 단위로 — 도착 칸에서 다시 뜬다**: 11의 결과로 보드 클릭이 반쪽이 됐다.
   초록 칸이 늘 기물의 *현재 위치* 기준이라 한 번 클릭하면 그걸로 끝이었고, 추가 이동은 패널의
   드롭다운으로만 지정할 수 있었다. `actionGeometry.ts`에 `movePlanCursor`를 추가해 "지금 클릭하면
   어느 구간을 채우는가"(구간 번호 / 출발 칸 / 그 구간의 상한 / 이미 확정된 앞 구간들)를 계산하고,
   `computeMoveOptions`가 그 **출발 칸**에서 초록 칸을 그리도록 했다. 클릭은 `applyMoveOption`으로
   앞 구간을 보존한 채 해당 구간 하나만 직진으로 채운다. 결과적으로 dealer2는 `기본 이동 → 도착 칸에서
   초록 칸 재등장 → 기술 1회 → 다시 재등장 → 2회 → 3회`를 보드 클릭만으로 이어서 찍을 수 있다.
   구간을 전부 채우면 커서가 기본 이동으로 되돌아가 보드 클릭만으로 경로를 갈아엎을 수도 있고,
   구간이 하나뿐인 일반 기물은 항상 커서가 0번이라 기존 동작과 완전히 동일하다.
   보드 힌트도 `초록색 칸을 클릭하면 기술 n회째 추가 이동을 지정합니다`로 커서를 따라 바뀐다.
13. **추가 이동의 칸수는 고를 수 없다 — 무조건 이동 Lv, 장애물이 있으면 그 앞 칸까지**: 9의
   "구간을 덜 쓸 수 있다"(기술1 왼쪽 2칸 예시)를 사용자가 다시 뒤집었다. 추가 이동은 한 번 쓰기로
   했으면 **통째로 이동 Lv(dealer2는 3칸)** 만큼 가고, 짧아지는 유일한 이유는 판 끝이나 장애물이다.
   `movePath.ts`에 `staticRunLimit`(방향으로 상한까지 걸으며 판 경계·장애물에서 멈춘 칸수),
   `staticStep`, `segmentOrigins`(각 구간의 출발 칸)를 추가하고 `validation.ts`가 1번 구간부터는
   `declared[seg] === staticRunLimit(구간 출발 칸, 방향, 상한)`인지 검사한다. 여기서 **다른 기물은
   일부러 보지 않는다** — 기물은 이번 턴에 같이 움직이므로 기물에 막히는 것은 계획을 불법으로 만들
   이유가 아니고, 해결 단계에서 그 앞에 멈추면 그만이다.
   UI도 규칙을 그대로 따른다: 추가 이동 구간의 칸수 select는 사라지고 `3칸 고정`(막히면 `2칸 고정` +
   구간 머리말에 `— 장애물 앞까지`)이 표시된다. 보드 클릭 후보도 `computeFixedMoveOptions`가
   **방향마다 딱 한 칸**만 내놓는다. 그리고 어떤 편집이든 `normalizeMoveSegments`를 거치게 해
   기본 이동 칸수를 줄여 출발 칸이 밀리면 뒤 구간 길이가 전부 자동으로 다시 계산된다 — 그렇지 않으면
   편집기가 엔진이 거부할 계획을 만들어 낸다.

**테스트 갱신**: 신규 `test/engine/rewindPath.test.ts` 13건(**한 번의 이동은 한 방향** 4건 — 기술 없는
기물의 꺾인 경로 거부 / tank2 돌진의 1회 전환 허용 / 2회째 전환 거부 / 합법적으로 꺾인 경로도 차단되면
그 지점에서 정지, 충전 1개=6칸, 추가 칸이 다음 턴으로 새지 않음, 충전 3개=12칸 이동 후 복귀, 공격 이후
복귀로 그 턴 피해까지 원복, 사망 시 기준점 제거·부활 시 충전 복구, 소모량 초과 이동 계획 거부,
**막힌 것 없이 추가 이동을 2칸으로 줄인 `[3,2,3,3]` 거부**, **장애물 때문에 2칸이 된 같은 경로는 허용**,
**구간 *안에서* 꺾는 dealer2 경로 거부**, **한 구간에 칸을 몰아쓰는 `[3,0,0,9]` 거부**).
`tsc --noEmit` / `npx vitest run`(45개 전부 통과) / `npm run build` 모두 통과. 추가로 개발 서버에서
드래프트→배치→계획→해결까지 실제로 클릭해, 사용자 예시대로 `기본 아래3 / 기술1 왼쪽2 / 기술2 아래3 /
기술3 오른쪽3`을 구간별로 입력하면 패널이 `기본 이동 도착 (5,3)` → `기술 1회 도착 (3,3)` →
`기술 2회 도착 (3,6)` → `기술 3회 도착 (6,6)`을 보여주고, 해결 결과
(`move … to (6,6), cellsMoved: 11`, `[3.공격] ⟲ 시간 역행 복귀 — p1:dealer2 · (6, 6) → (5, 0)`)가
미리보기와 정확히 일치하는 것을 눈으로 확인. 방향 규칙 변경 후에도 같은 화면에서 dealer2는 구간마다
`방향 + 칸수` 한 쌍만 갖고(경로 변경 버튼 없음) 네 도착점이 그대로 나오는 것, tank2는 `+ 경로 변경`으로
`꺾어서` 행이 추가되며 기본 방향이 벽 쪽이 아닌 실제로 갈 수 있는 방향으로 잡히는 것을 재확인.
구간별 초록 칸도 브라우저에서 확인: (4,1)의 dealer2가 기술 3회를 켠 상태에서 `(4,4) 클릭 → (4,4)
기준 초록 칸 재등장 → (6,4) → (6,4) 기준 재등장 → (6,7) → 재등장 → (5,7)`로 네 구간이 순서대로
채워지고, 패널이 `기본 (3/3) → 기술1 (2/3) → 기술2 (3/3) → 기술3 (1/3)`, 보드가 `기본·기술1·기술2·기술3`
표식을 그대로 보여줬다. 칸수 고정 규칙도 정원 맵에서 확인: (5,0)의 dealer2가 기본 아래2로 (5,2)에
선 뒤 추가 이동 후보가 방향마다 한 칸씩(`위 (5,0)` = 판 끝, `왼쪽 (3,2)` = 장애물 (2,2) 앞,
`오른쪽 (8,2)`, `아래 (5,5)`)만 뜨고, 왼쪽을 고르면 패널이 `기술 1회 (2/3칸) — 장애물 앞까지` /
`2칸 고정`을 보여줬다. 기본 이동을 2→3칸으로 늘리자 뒤 구간이 자동으로 다시 계산돼
`기술 1회 도착 (5,5)`가 `(5,6)`으로 따라 움직였다.

### 8) 기술 이동 후 공격 ("기본 행동이 공격이면 기술 3번만 이용해서 이동")

기본 행동은 이동/공격 중 하나뿐이라(§3.2) 한 턴에 "움직여서 붙은 뒤 때린다"를 표현할 방법이 없었다.
처음에는 *기본 이동에 공격을 덧붙이는* `followUpAttack` 부가 행동으로 만들었는데, 그러면 dealer2가
`기본 이동 3 + 기술 3×3 + 공격` = **이동 12칸 + 공격**이 되어 버렸다. 사용자 지적("이동 이동 이동 이동
공격으로 된다")에 따라 모델을 뒤집었다 — **기본 행동이 공격이면 기본 이동 몫은 아예 없고, 이동은 오직
"이동을 한 번 더" 주는 기술이 준 구간(3회 × 3칸 = 9칸)뿐이다.**

1. **자격 규칙(`engine/movePath.ts`의 `canPlanSkillMove`)**: `wholeExtraMoveUses > 0`인 기술, 즉
   **이동을 통째로 한 번 더 주는** dealer2 시간역행만 해당한다. tank1/tank2의 `이동 +1`은 기본 이동을
   늘리는 가산 버프라 기본 행동이 공격이면 늘릴 이동 자체가 없다(예전 `engine/followUp.ts`는 삭제).
2. **계획 표현**: `UnitTurnPlan.skillMove?: { path, segmentLengths }` — 기본 행동(공격)은 그대로 두고
   경로만 따로 담는다. 핵심 트릭은 `normalizedMoveAction()`이 이를 **기본 구간이 0칸인 이동**
   (`segmentLengths: [0, 기술1, 기술2, …]`)으로 정규화한다는 것 — 구간 상한·장애물 절단·미리보기·검증이
   전부 기존 이동 코드를 구간 번호까지 어긋남 없이 그대로 탄다. `sanitizePlan`은 이 항목만 불법일 때
   계획 전체를 버리지 않고 `skillMove`만 떼어 내고, 스토어도 기본 행동을 이동으로 바꾸거나 이동을 만들지
   못하는 기술로 바꾸면 자동으로 지운다.
3. **해결**: 이동은 1단계, 공격은 3단계다. `resolvers/movement.ts`가 기술 이동을 실행할 때 상한을
   `기술 몫`으로만 잡고(`isSkillOnlyMove`), 공격 단계에 오면 `unit.position`이 이미 도착 칸이라
   사거리·방벽·아군 차단·피해 계산이 기본 공격과 **완전히 같은 코드**를 탄다. dealer2의 복귀는 공격
   직후라(7-4) `도착 칸에서 공격 → 기준점으로 복귀`하는 히트&런이 공짜로 성립한다.
4. **UI**: 기본 행동을 공격으로 고르고 시간역행을 쓰면 편집기에 `⇢ 기본 행동이 공격이라 이동은 전부
   기술 몫입니다 — 기본 이동(3칸)은 쓰지 않고 기술이 준 9칸(3회 × 3칸)만…` 안내가 뜨고, 경로 편집기는
   **기본 이동 구간(0번)을 감춘 채** 기술 1·2·3회 구간만 보여준다. 보드의 초록 칸도 기술 구간부터
   찍히고(`movePlanCursor`의 `firstEditable`), 주황 칸은 **도착 칸 기준**으로 그려진다. 클릭 라우팅은
   `movePlanFromSegments(…, isSkillMovePlanning(plan))` 한 곳에서 `setBaseAction`/`setSkillMove`로
   갈린다. 접힌 행 요약은 `공격 위 + 추가 이동 + 기술 이동 아래 9칸 (3회)`, 로그는
   `⇢ 기술 이동 후 공격 — p1:dealer2 · (7, 10)에서 위 방향`.
   기본 행동이 '없음'일 때는 예전처럼 평범한 기본 이동을 계획한다(공격일 때만 기술 이동 모드).

**테스트 갱신**: `test/engine/followUpAttack.test.ts` → 신규 `test/engine/skillMoveAttack.test.ts` 5건
(기본 이동 없이 기술 3칸만 가서 도착 칸에서 공격 / 시간역행 2회 = 6칸이지 9칸이 아님 / 히트&런 — 피해는
남고 몸은 기준점 복귀 / 이동을 만들지 못하는 기술이면 `skillMove`만 떨어져 나가고 제자리 공격 / 기본
행동이 이동이면 경로는 기본 행동이 갖고 공격은 없음). `tsc --noEmit` / `npx vitest run`(57개 전부 통과) /
`npm run build` 통과. 브라우저에서도 (7,1) dealer2로 `공격 + 시간역행 3회`를 잡자 상한이 **9칸**으로
표시되고(12칸이 아님), 초록 칸을 세 번 찍어 (7,4)→(7,7)→(7,10)을 만든 뒤 해결하니
`[3.공격] ⇢ 기술 이동 후 공격 — p1:dealer2 · (7, 10)에서 위 방향` →
`[3.공격] ⟲ 시간 역행 복귀 — (7, 10) → (7, 1) · 충전 초기화`가 찍히는 것을 확인.

### 9) 맵 1.5배 확대 ("맵의 크기를 1.5배 크게 만들어줘")

1. **크기**: 9×13 → **13×19**. 각 변에 1.5를 곱하면 13.5×19.5라 정수가 아니므로 **홀수를 유지하도록
   내림**했다 — 폭이 홀수여야 중앙 열(x=6) 기준 좌우가, 높이가 홀수여야 중앙 행(y=9) 기준 상하가 완전히
   대칭이 되어 양 팀 진입 조건이 같아진다. 14×20으로 올리면 중앙이 두 칸이 되어 대칭이 깨진다.
2. **6개 구역**: 역할과 서로의 비율은 그대로 두고 치수만 늘렸다(`data/mapDefinitions.ts`).
   점령지는 한 번 5×5로 늘렸다가 **사용자 요청으로 원래 3×3((5,8)~(7,10), 중심 (6,9))로 되돌렸다** —
   맵이 넓어진 만큼 점령지가 상대적으로 좁아져 한 칸을 두고 다투는 밀도가 올라간다.
   시작지점 2행 → **3행**(p1 y=0..2 / p2 y=16..18, 좌우 1칸 여백),
   좌측 골목은 폭 1칸을 유지한 채 코너 1곳 → **2곳**(x=1 → x=3 → 다시 x=1로 지그재그),
   우측 다리 폭 2칸 → **3칸**(x=9..11, y=6..12), 공터 엄폐물 2개 → **3개**((12,5)·(12,9)·(12,13)).
3. **화면 맞춤**: 546×798px가 되어 세로가 화면을 넘치므로 `Board.tsx`에 `viewBox`를 추가하고
   `.board-svg`를 `max-height: calc(100vh - 120px)`로 묶어 비율 그대로 축소되게 했다. 이때 보드 열이
   같이 줄어들어 폭에 눌리지 않도록 `.board-column`에 `flex: 0 0 auto`를 줬다. `CELL_SIZE`(42)는
   그대로라 기물 토큰·HP바·스텝 번호의 상대 크기는 손대지 않았다.
4. **회귀 테스트**: 좌표를 손으로 적는 파일이라 한 칸만 틀려도 통로가 막히거나 한쪽만 유리해질 수 있어
   `test/engine/gardenMap.test.ts` 7건을 새로 뒀다 — 크기 13×19 / 시작지점과 점령지 비겹침·장애물 없음 /
   점령지가 (6,9) 중심 3×3 연속 구역 / **장애물의 상하 대칭**(y → height-1-y) / 양 시작지점과 점령지의
   연결성 / x≥4를 전부 막아도 좌측 골목만으로 반대편 도달 / x≤8을 전부 막아도 우측 다리·공터만으로
   반대편 도달(= 중앙 진입로가 최소 2개라는 기획서 4장 요건).

`tsc --noEmit` / `npx vitest run`(8파일 57개 전부 통과) / `npm run build` 통과. 브라우저에서도 드래프트
→ 배치(p1 y=0행, p2 y=18행) → 전투 시작까지 진행해 247칸(13×19)이 그려지고, 점령지 9칸이
(5,8)~(7,10), 우측 엄폐물이 (12,5)·(12,9)·(12,13)에 있으며 보드가 439×641px로 비율 그대로 축소되는
것을 확인.

### 10) 보드 클릭으로 공격 방향 지정 ("공격시 클릭해서 공격의 방향을 설정했으면 좋겠어")

기존에는 공격 방향을 패널의 위/아래/왼쪽/오른쪽 버튼으로만 정할 수 있었다. 보드에는 주황 칸이 이미
그려져 있었지만 클릭하면 **항상 이동으로 해석**됐다 — 이동 칸과 공격 칸이 겹치는 칸에서 초록(이동)이
색·클릭 처리 양쪽에서 무조건 이겼기 때문이다. 적 기물을 클릭해도 "그 기물 선택"으로만 처리됐다.

1. **보드 클릭 모드 토글**(`App.tsx`의 `clickMode: 'move' | 'attack'`, UI는 `.click-mode-switch`의
   `이동 지정` / `공격 방향 지정` 두 버튼). 겹친 칸의 의미를 "숨은 우선순위"가 아니라 **눈에 보이는
   모드**가 결정하게 했다. 기물을 새로 선택하거나 기본 행동이 바뀌면 모드도 따라간다
   (`baseAction.kind`가 attack/attackAt이면 자동으로 공격 모드).
2. **색이 클릭 결과를 그대로 반영**(`Board.tsx`의 `clickPriority` prop). 겹친 칸은 지금 모드 쪽 색으로
   칠하고, 테두리도 **모드 쪽을 나중에 그려** 위에 보이게 하고, `<title>` 툴팁 문구도 같이 뒤집는다 —
   "이 칸을 클릭하면 무슨 일이 일어나는가"를 보드만 보고 읽을 수 있게 하는 것이 이 기능의 핵심이다.
3. **적 기물 클릭 = 조준**(`handleUnitClick`). 공격 모드에서 사거리 안의 적 토큰을 클릭하면 선택을
   바꾸지 않고 그 방향으로 공격을 지정한다. 사거리 밖이거나 이동 모드면 기존대로 기물 선택.
4. **"이동 지정" 버튼이 잠기는 막다른 길 해결**. 기본 행동을 공격으로 잡으면 기본 이동 몫이 사라져
   초록 칸이 0개가 되고, 그러면 이동 모드 버튼이 `disabled`가 되어 **보드만으로는 공격 계획을 되돌릴
   방법이 없어진다**. 그래서 초록 칸은 항상 **"이 기물이 지금 이동을 계획한다면"**을 기준으로
   계산하고(`movePlanBasis`), 초록 칸을 클릭하면 공격 계획이 이동으로 교체된다. 안내문에도
   `초록색 칸을 클릭하면 잡아 둔 공격 계획 대신 그 칸까지 이동합니다.`로 그 사실을 명시한다.
   단 dealer2처럼 **기술로 이동할 수 있는 기물은 예외** — 공격을 유지한 채 기술 구간을 찍어야 하므로
   원래 계획을 그대로 쓴다(9장의 "기술 이동 후 공격"과 충돌하지 않게).

`tsc --noEmit` / `npx vitest run`(57개 전부 통과) / `npm run build` 통과. 브라우저 확인: tank1(3,0)에서
공격 모드로 (3,2) 주황 칸 클릭 → `공격 아래`, 이 상태에서도 `이동 지정` 버튼이 살아 있고 초록 칸
클릭 시 `이동 왼쪽 1칸`으로 교체됨. dealer2(7,0)는 공격+시간역행 상태에서 (7,3)을 클릭해
`공격 아래 + 추가 이동 + 기술 이동 아래 3칸 (1회)`로 공격을 유지한 채 기술 구간만 지정됨.
두 턴을 해결해 p1 tank2(4,8)와 p2 tank2(4,10)를 마주 세운 뒤 **적 토큰을 직접 클릭**하니 선택이
바뀌지 않고 `공격 아래`가 지정되는 것까지 확인.

### 11) tank2 돌진 피해 — "경로의 적"을 관통 판정으로 재구현

기획서 §7.1의 "경로의 적에게 이동 칸수만큼 피해"를 이전에는 **경로에 인접한 적 전원**으로 구현했었다.
문자 해석과도 다르고, 5칸 돌진이 복도 양옆 전원에게 5씩 넣는 최강 광역기가 되어 밸런스도 깨졌다.
근본 원인은 **일반 이동이 적 칸을 통과할 수 없어 "경로 위의 적"이 성립하지 않는다**는 점이었다.

1. **돌진 중에만 적을 통과**(`resolvers/movement.ts`). 스텝 루프에서 정적 지형은 그대로 막고,
   점유 기물은 **적일 때만** 밟고 지나간다(아군은 막힘 — 아군 통과는 돌진이 아니라 순간이동이다).
   밟고 지나간 적은 순서대로 `trampled`에 쌓는다.
2. **밟은 적 위에서는 멈출 수 없다.** 정지 칸이 점유돼 있으면 비어 있는 마지막 칸까지 되돌리고,
   `movedCells`와 `trampled`를 **함께** 잘라낸다 — 그래야 "피해량 = 실제 이동 칸수"가 어긋나지 않고,
   지나가지도 못한 적이 맞는 일이 없다.
3. **피해는 이동이 다 끝난 뒤 일괄 적용.** 스텝마다 즉시 적용하면 "밟아 죽인 칸에 설 수 있는가"에 따라
   이동 칸수(=피해량)가 달라져 순환 참조가 된다. 채택한 기본값: **밟아 죽여도 그 칸을 차지하지 못한다.**
4. **판정 조건 정밀화**: `isDash`가 `typeId === 'tank2' && skillUse?.skillId === 'tank2_charge'`를 본다
   (이전에는 tank2가 아무 기술이나 쓰면 돌진으로 쳤다). 기본 행동이 공격인 기술 이동에는 붙지 않는다.
5. **보드 하이라이트도 같은 통과 규칙으로**(`engine/grid.ts`, `Planning/actionGeometry.ts`).
   초록 칸은 `reachableSteps`로 그리는데 이게 기물을 무조건 막아, 돌진인데도 적 뒤쪽 칸이 안 떠서
   **계획 자체가 불가능**했다. `reachableSteps`에 `passThrough` 술어를 넣고 반환 타입을
   `{position, distance}[]`로 바꿨다 — 통과만 하는 칸이 생기면 배열 index와 실제 이동 칸수가
   달라지기 때문이다. `isDashPlanning()`이 엔진의 `isDash`와 같은 조건을 보므로
   "초록으로 떴는데 실제로는 못 간다"가 생기지 않는다.
6. **미정 항목 결정**: 돌진 피해는 **방벽(barrier)을 무시**한다(방벽 체크는 `attacks.ts`의 직선 공격에만
   존재 — 범위공격이 방벽을 관통하는 기존 규칙과 같은 결). **보호막(shieldHp)은 흡수**한다.
   또한 돌진 피해는 **1단계**에서 들어가므로, 죽은 적은 3단계 공격 순서 계산에서 아예 빠져
   그 턴 공격을 하지 못한다.
7. **UI**: 경로 편집기 위에 붉은 `.dash-note`("적을 밟고 지나가며 … 아군과 장애물은 그대로 막고 …"),
   해결 로그에 `⇉ 돌진 관통 — p1:tank2 → p2:tank2 · 이동 칸수만큼 4 피해` 전용 줄(`.log-dash`).

`tsc --noEmit` / `npx vitest run`(9파일 66개 통과) / `npm run build` 통과. 신규
`test/engine/dashDamage.test.ts` 9건 — 일렬 관통 2명 동시 피해 / 정지 칸이 막히면 한 칸 물러나며
피해량도 줄어듦 / 아군 벽 앞 정지 / 장애물은 관통 불가 / 돌진사한 적은 그 턴 공격 불가 /
기술 없는 이동은 관통·피해 없음 / 하이라이트 3건(적 뒤 칸 노출·적 칸 제외·distance 정확성).
브라우저에서도 (4,14)의 tank2가 (4,16)의 적을 밟고 (4,18)까지 4칸 이동해 적 HP 30 → 26,
로그에 돌진 관통 줄이 뜨는 것을 확인.

### 12) 대전 모드 3종(로컬·AI·온라인) + 배포

"온라인 멀티, AI 대국 구현해서 배포까지" 요청. 이전까지는 **한 화면에서 한 사람이 양쪽을 다 조작하는
디버그 모드 하나뿐**이었다. 세 모드를 붙이면서 "누가 무엇을 조작할 수 있는가"를 처음으로 명시적인
상태(`mode` + `localOwner`)로 만들었다.

**모드/메뉴 (`store/gameStore.ts`, `components/Menu/ModeMenu.tsx`)**
- `stage: 'menu' | 'draft' | 'placement' | 'game'`를 새로 두고 메뉴를 첫 화면으로 삼았다.
- `mode: 'local' | 'ai' | 'online'`, `localOwner`(내가 조종하는 진영)를 스토어에 추가.
  화면 곳곳의 "양쪽 다 보여줄까?" 판단이 전부 이 두 값 하나로 통일된다 —
  `planningOwners = mode === 'local' ? ['p1','p2'] : [localOwner]` (App), 드래프트 열의 `readOnly`,
  배치 화면의 `owners`, 보드 클릭 허용 여부(`controlsSelected`)까지.

**AI (`ai/aiPlayer.ts`, `ai/difficulty.ts`)**
- **트리 탐색을 하지 않는다.** 동시 턴 게임이라 상대 계획을 알 수 없어 미니맥스의 이득이 작고,
  후보가 기물 5개 × (방향 × 거리 × 기술 조합)으로 폭발한다. 대신 기물마다 후보 계획을 만들어
  **한 수 평가**하고, 협동은 공통 목표항(점령지 거리·적 위치·위협)을 통해 간접적으로 나오게 했다.
- **난이도 = 탐색 깊이가 아니라 "어떤 실수를 얼마나 자주 하는가"**(`DifficultyProfile`의
  `noise` / `useSkills` / `captureWeight` / `threatWeight` / `killWeight`). 셋 다 평가 함수는 하나를 공유한다.
- **불법 계획이 구조적으로 나올 수 없다.** 모든 후보는 엔진의 `isActionLegal`을 통과해야만 채택되고,
  최종 계획은 `sanitizePlan`을 한 번 더 거친다. AI가 규칙을 잘못 모델링하면 그 후보가 조용히
  탈락할 뿐, 엔진이 거부할 계획이 나가지 않는다.
- 평가 함수는 엔진을 **그대로 흉내 낸다** — `attackValue`는 `attacks.ts`(아군이 사선을 막음, 방벽 무효화,
  aoe 띠 전체 타격, dealer4 측면 +7)를, `walkLine`은 `resolvers/movement.ts`(돌진 관통 + 점유 칸에서
  한 칸 물러나기)를 미러링한다. 여기가 어긋나면 AI가 "될 줄 알았던 수"를 계속 고른다.
- dealer2는 **충전이 2 이상일 때만** 추가 이동을 쓴다 — 0에 닿으면 시간역행으로 되돌아가는 함정을
  AI가 스스로 밟지 않게.
- AI 대전에서는 편성·배치도 AI가 한다(`aiDraftPicks`, `aiPlacement` — 탱커가 점령지에 가장 가깝게).

**온라인 (`online/netBridge.ts`)**
- PeerJS(WebRTC) P2P. 방 코드 6자리(`simultaneous-<CODE>`가 peer id), 서버 없음.
- **방장 권위(host-authoritative)**: 게스트는 자기 스토어를 절대 직접 바꾸지 않고 `RemoteAction`을
  방장에게 보낸다. 방장이 적용한 뒤 `StoreSnapshot` 전체를 되돌려 보낸다. 이렇게 한 이유는
  **무작위 요소(우선순위 동률 타이브레이크)가 양쪽에서 따로 굴러 화면이 갈라지는 것**을 막기 위해서다.
  이 게임은 설계상 숨겨진 정보가 없어(양쪽 계획을 동시에 공개) 상태를 통째로 공유해도 규칙상 손해가 없다.
- 구현은 스토어 액션마다 한 줄(`if (forwardIfGuest({...})) return;`)로 끝난다 — 분기가 액션 안에 있으므로
  UI 컴포넌트는 온라인 여부를 몰라도 된다.

**배포**
- `vite.config.ts`에 `base: './'`(상대 경로) — 저장소 이름이 무엇이든, 루트 도메인에 올려도 동작.
- `.github/workflows/deploy.yml`: main 푸시 → `npm ci` → `npm test` → `npm run build` →
  `upload-pages-artifact` → `deploy-pages`. **테스트를 빌드보다 먼저** 돌려 깨진 엔진이 배포되지 않게 했다.
- `.gitignore`에 `dist`, `*.tsbuildinfo`, `vite.config.js`, `vite.config.d.ts`, `.claude` 추가(전부 산출물/로컬 설정).
- `README.md` 신규 작성(규칙 요약, 3모드 사용법, AI 설계 근거, 배포 절차).

**실버그 수정**: `createInitialState.ts`의 `createUnitInstance`가 `typeDef.isTurret`을 인스턴스에
**한 번도 복사하지 않고 있었다**. 그래서 (1) `healing.ts`의 포탑 회복 오라가
`units.filter(u => u.isTurret)`에 아무것도 걸리지 않아 **아예 돌지 않았고**, (2) 포탑이 계획 패널에
끼어들 수 있었다. AI 테스트("포탑은 계획 대상이 아니다")가 이 오래된 버그를 처음 드러냈다.

**UI 보강**: 연결이 끊겨도 화면은 그대로 남아 상대가 나간 줄 모르고 혼자 계속 두게 되므로,
`ModeBanner`가 메뉴 밖(드래프트·배치·대전)에서도 `online.error`/`connecting`을 표시하게 했다
(`.mode-banner-alert`). 기존에는 이 오류가 메뉴 화면에서만 렌더됐다.

`tsc --noEmit` / `npx vitest run`(10파일 79개 통과) / `npm run build` 통과. 신규
`test/ai/aiPlayer.test.ts` 13건 — 3난이도 × 10기물 전원에 대해 **엔진이 거부하는 계획이 하나도 없음** /
죽은 기물·포탑 제외 / 구속 시 이동 계획 없음 / 마무리 사격 / 사거리에 아무도 없으면 점령지로 전진 /
tank2 일렬 관통 돌진 선택 / dealer3 공격모드 켜고 사격 / support2 회복 / 쉬움은 기술 미사용 /
드래프트 5기물 / 배치 규칙 / AI끼리 20턴 무예외 진행 + 실제 피해 발생 / 무행동 상대에게 60턴 내 득점.
브라우저 확인: 어려움 AI가 스스로 편성·배치한 뒤 무행동 상대를 상대로 **9턴 만에 11:0 승리**
(4기물을 점령지에 밀어 넣음), 온라인은 방 코드 발급 → 게스트 접속 → 게스트가 보낸 `togglePick`이
방장에 적용되고 스냅샷이 되돌아오는 왕복까지 실제 P2P 연결로 확인.

### 13) 대상 지정 기술의 사거리 강제 + support2 재설계("조준 보조" 신규 기술)

**사거리가 장식이었다.** `payload.range`는 **아무 데서도 읽히지 않고 있었다**. 해결 단계는 instanceId로
대상을 찾아 거리 검사 없이 적용했고, validation은 쿨타임·충전만 봤으며, UI는 살아 있는 기물 전체를
드롭다운에 넣었다. 그래서 "회복 직선 4칸"인 support2가 13×19 맵 어디로든 회복했고, "대각선 3칸"
자리교체인 dealer4는 사실상 무제한 순간이동이었다. **데이터의 사거리를 4에서 7로 올려도 시뮬레이션
결과가 한 자리도 안 변해서** 발견했다 — AI만 `chebyshev <= 4`를 하드코딩해 두고 있었고, 그 숫자가
사실상의 규칙이었다.

- 축·사거리의 단일 근거를 `engine/skillRange.ts`(`SKILL_AXIS`)에 모으고, 네 소비처(해결 단계·
  validation·AI·UI)가 전부 여기서 읽게 했다. 숫자를 네 군데가 따로 들고 있던 것이 버그의 원인이었다.
- **사거리는 두 번 본다.** validation은 **계획한 도착 칸** 기준으로(출발 칸으로 재면 "라인을 열려고
  옆으로 한 칸 서는" 정상적인 수가 전부 불법이 된다), 해결 단계는 이동이 끝난 뒤 **다시** 본다
  (계획 시점에 닿았어도 양쪽이 움직여 벌어졌을 수 있다).
- UI는 사거리 밖 대상을 **지우지 않고 비활성**으로 남긴다("— 사거리 밖"). 목록에서 빼 버리면
  "왜 저 기물을 못 고르지?"에 답이 없다.
- tank3 구속은 기획서에 사거리가 없어 기본값을 정했다: **직선·대각선 3칸**(쿨타임 5턴으로 길고,
  이동 2의 근접 기물이라 직선만으로는 실전에서 거의 못 쓴다).

**support2를 회복으로 살리려는 시도는 두 번 다 실패했다.** 이동력 1이 정체성이라 기동력으로는 못
올리고, 대신 회복을 키우려 했는데:

| 시도 | 결과 |
|---|---|
| 직선 유지 + 사거리 4→12 (13×19에서 사실상 무제한) | 38.8% → **38.2%** (하락) |
| 직선 유지 + 힐량 8→25 (3배) | 판당 회복량만 정확히 3배, 승점 **38.2%** |
| 자기중심 반경으로 축 변경 | 44.7%로 오르지만 **저격수 힐러 컨셉이 무너짐** → 폐기 |

**원인은 회복의 세기가 아니었다.** 기회를 분해해 보니(`scripts/support2Opportunity.ts`) 사거리 안에
아군이 있는 턴은 **67%**인데 그중 **부상한** 아군이 있는 턴은 **9%**뿐이었다. 58%의 턴에서 이 기물은
아군을 빤히 보면서도 "회복은 다친 아군에게만 걸린다"는 **대상 조건** 때문에 아무것도 못 하고 있었다.
축을 대각선까지 열어도 부상 아군은 9→15%로밖에 안 올라, 기하로는 못 고치는 문제였다.

**해법: 신규 기술 "조준 보조"**(`support2_buff`, 사선 위 아군 1명 공격력 +3, 사거리 직선 6).
멀쩡한 아군에게도 걸리므로 9%가 아니라 **67%의 풀**을 쓴다. 회복의 사거리·축·힐량은 기획서 원문
그대로 한 글자도 안 건드렸다.

- 실측: 실질 가동률(회복+버프) **10% → 45%**, support2 승점 **38.8% → 44.0%**(hard) / 46.4%(normal),
  전체편차 23.7 → **15.9%p**. 최하위에서 벗어났다.
- **버프량이 3인 건 상한이 아니라 하한 때문이다.** 6까지 올리면 승점은 46.0%로 더 높지만 판당
  회복량이 17.1→**1.1**로 무너진다 — 버프 가치(6×1.5)가 회복 가치(3×2.5)를 항상 이겨서 이 기물이
  회복을 아예 안 하는 버프 봇이 된다. 3이면 회복이 먼저고 버프는 **빈 턴만 메운다**(판당 회복량 16.0 유지).
- 공격력을 읽는 곳이 여럿이라 버프는 `unitStats.ts` 한 곳에서만 더한다(사거리 버그와 같은 실수 방지).

**분석 도구 3종 추가**(전부 앱 번들 밖): `scripts/tuneSupport2.ts`(통제된 스윕 — 모든 변형이 같은 시드
집합으로 같은 편성·배치를 만난다), `scripts/support2Opportunity.ts`(기회 분해), `scripts/pairSynergy.ts`
(**이중차분** 짝 시너지 — 단순 비교는 "X가 그냥 센 기물"인 것과 구분이 안 돼서, support2 없는 편성을
통제군으로 빼야 순수 상호작용이 남는다).

**알려진 한계**: AI는 `dealer4_swap`을 **한 번도 계획하지 않는다**(`src/ai/`에 참조 없음). 그래서
"dealer4가 이동력 1인 support2를 사선 위로 순간이동시킨다"는 이론적 시너지는 시뮬레이션에 절대 안
잡힌다 — 이중차분 실측 -1.3%p는 **하한**이지 상한이 아니다. 실제로 측정된 유일한 양의 시너지는
방어 강화형(+6.9%p)과 충전 사격형(+3.4%p)인데, 둘 다 **느리거나 아예 못 움직이는**(공격모드 중
dealer3는 이동 불가) 기물이라 사선을 맞춰야 하는 힐러에게 안정적인 대상이 된다는 해석과 맞는다.

### 14) support2 공격 부여안 기각 + AI 위협 계산(`threatAt`) 재작성

**제안**: support2에 공격을 준다 — 딜은 힐량의 1/2(8→4), 사거리는 회복과 같은 직선 4.

**1차 측정은 대성공처럼 보였다.** 300판 hard에서 support2 승점 44.0 → **51.1%**, 9위 → 5위. 처음으로
목표 50%를 넘겼다. 그런데 **판당 피해가 1.0**이었다. 이게 걸려서 통제 실험을 했다.

| support2 공격 | 승점률 | 판당피해 | 판당회복 | 판당사망 |
|---|---|---|---|---|
| 없음(기획서 원안) | 44.0% | 0.0 | 15.5 | 0.19 |
| 공격력 1 · 직선 4 | 45.5% | 0.2 | 15.4 | 0.14 |
| 공격력 4 · 직선 4 | 51.1% | 1.0 | 19.8 | 0.11 |

공격력 1→4로 **실제 피해는 판당 0.8 늘었는데 승점은 5.6%p 올랐다**. 장거리 화력형이 판당 7.2 피해로
50.8%인 걸 감안하면 성립할 수 없는 비율이다. 절제 실험으로 확정했다 — 공격력 4를 그대로 둔 채
**적 AI의 위협 계산에서만 support2를 빼자 44.3%로 복귀**(판당 피해는 1.2로 그대로). 즉 상승분 7.1%p는
전부 "적이 못 맞히는 총을 무서워해서 접근을 안 한 것"이었다. 기물 데이터는 원안(공격 불가)으로 되돌렸다.

`scripts/support2Opportunity.ts`에 공격 칸 두 개(`사선내적`/`실제공격`)를 추가해 왜 못 맞히는지도 확인했다:
사선 안에 적이 있던 턴 21%, 실제로 공격을 고른 턴 10% — 기회의 절반은 쓴다. 문제는 **명중률 14%**다
(판당 1.77회 쏴서 피해 1.0). 동시 턴이라 이동 **전** 위치를 보고 조준하는데 이동력 1이라 빗나가도
재조준을 못 한다. 게다가 회복과 택1(§3.2)이라 쏘는 턴은 힐을 버리는 턴이다.

**진짜 문제는 `threatAt`이었다.** 이건 support2만의 문제가 아니라, 어떤 기물의 공격 스탯을 만져도 같은
착시가 승점에 섞여 들어온다는 뜻이라 함수를 다시 썼다(`src/ai/aiPlayer.ts`). 고친 결함 네 가지:

1. **맞는 쪽의 이동력을 쓰고 있었다** — `plannedMoveSpeed(unit)`(위협받는 기물)이 들어가 있었다.
   발 느린 기물은 적을 실제보다 덜 무서워하고 발 빠른 기물은 더 무서워하는, 정확히 반대 방향의 버그다.
2. **축을 무시했다** — 체비쇼프 거리 하나로만 재서, 직선 공격 기물이 자기 행·열이 아닌 칸까지 주변
   사각형 전체를 위협하는 것으로 계산됐다. 이게 착시의 주범이다. 새 `stepsToLineUp()`이 "조준선에
   올리기까지 필요한 최소 이동 칸 수"를 축별로 계산한다. 대각선 이동이 가능한 기물은 한 걸음에 조준
   오차를 2씩 줄일 수 있어 이동 방식으로 갈라 계산한다(처음엔 이걸 빼먹어 dealer4를 3칸으로 잘못 셌다).
3. **기본공격 쿨타임을 안 봤다** — dealer1은 쉬는 턴에도 만점 위협으로 계산됐다(당시 규칙은 "쏘면
   3턴 못 쏨"이었고, 아래 §14에서 탄창식으로 바뀌었다).
4. **dealer3의 제약을 안 봤다** — 이동과 공격이 같은 턴에 절대 불가한데(validation.ts) 이동력만큼
   다가와서 쏠 수 있는 것으로 봤다. 구속당한 적도 마찬가지로 접근 불가 처리.

1·3·4는 `validation.ts`가 강제하는 규칙을 그대로 옮긴 것이라 근사가 아니라 정확하다. 장애물과 다른
기물은 일부러 무시한다 — 막힐 수 있다는 이유로 위협을 깎으면 실제로 뚫렸을 때 그대로 죽으므로,
위협은 **상한**으로 잡는 편이 안전하다.

**검증**: 같은 공격 부여안을 고친 `threatAt` 위에서 다시 재니 51.1% → **40.0%**로, 착시가 사라졌을 뿐
아니라 부호까지 직관과 맞게 뒤집혔다(빗나가는 총을 힐과 맞바꾸면 소폭 손해). `src/test/ai/aiPlayer.test.ts`에
회귀 테스트 7개 추가 — 그중 5개는 기존 구현에서 실제로 실패한다.

**부수 효과(주의)**: AI가 덜 겁내게 되면서 판이 훨씬 공격적으로 바뀌었다. 판당 피해가 전반적으로
크게 늘고(dealer4 30.0→45.0, tank3 12.4→20.0), 점령지 체류와 회복량도 함께 올랐다(support1 회복
69.9→104.0). 순위도 재편됐다(hard 300판): 측면 교란형 59.0 / 돌진·기동형 56.5 / 충전 사격형 55.2 /
시간 역행형 54.2 / 범위 회복형 48.9 / 확률·포탑형 47.9 / 방벽·제어형 47.6 / 장거리 화력형 47.3 /
방어 강화형 44.7 / 장거리 회복·구속형 42.1. 전체 편차 16.9%p. **13)절까지의 밸런스 수치는 전부 옛
위협 모델 위에서 잰 값이므로 이 표와 직접 비교하면 안 된다.**

**맞대결 검증 (`scripts/aiDuel.ts`)**: "더 정확해졌다"와 "더 세졌다"는 같은 말이 아니다 — 위협을
정확히 보는 AI가 겁이 줄어 오히려 무모해질 수도 있다. 그래서 옛 모델을 `threatAt` 앞에 일시적으로
되살려 같은 판에서 붙였다. 1·3·4는 규칙 위반을 고친 것이라 논란의 여지가 없지만 2(축 인식)는
모델 개선이라, 이 검증이 있어야 결론이 단단해진다.

측정 설계에서 조심한 것 둘:

- **거울 편성** — 양쪽에 똑같은 무작위 5기물을 준다. 편성 운이 섞이면 300판으로는 ±5%p가 그냥
  노이즈다. 편성을 같게 두면 두 쪽의 차이가 위협 모델 하나로 좁혀진다.
- **진영 교대** — p1이 유리하다(아래 easy 대조군에서 53.3%로 측정됐다). 같은 시드로
  (p1=새/p2=옛)과 (p1=옛/p2=새)를 둘 다 돌려 합산하면 진영 이득이 상쇄된다.

| 난이도 | threatWeight | 새 모델 승 | 옛 모델 승 | 무승부 | 새 모델 승점률 |
|---|---|---|---|---|---|
| hard | 1 | 370 | 223 | 7 | **62.3%** (+12.3%p, 6.0σ) |
| normal | 0.35 | 347 | 252 | 1 | **57.9%** (+7.9%p, 3.9σ) |
| easy | 0 | 150 | 150 | 0 | 50.0% (±0.0%p) |

각 난이도 600판(easy만 300판). 새 모델이 **양쪽 진영 모두에서** 이겼으므로(hard 기준 63.7% / 60.2%)
진영 편향으로 설명되지 않는다. 위협 가중치에 비례해 격차가 벌어지는 것(1 → +12.3%p, 0.35 → +7.9%p,
0 → 0%p)도 원인이 위협 모델이라는 것과 앞뒤가 맞는다.

**easy 판은 대조군으로서 값어치가 크다.** `threatWeight: 0`이면 `threatAt`의 반환값에 0이 곱해져
사라지므로 두 AI는 *같은 수*를 둔다 — 실제로 150:150에 두 배치가 정확한 거울(70/80, 80/70)이고 평균
턴수까지 22.0으로 같았다. 판 자체가 비트 단위로 동일했다는 뜻이라, 스위치가 `threatAt` 말고 다른
경로로 새지 않았음이 증명된다. 덤으로 p1 이득이 80/150 = 53.3%로 깨끗하게 측정됐고, 진영 교대가
이것을 정확히 상쇄한다는 것도 같은 표에서 확인된다.

**스위치는 측정 후 제거했다.** AI 안에 살아 있는 전역 가변 플래그는 테스트를 조용히 오염시킨다.
`scripts/aiDuel.ts`는 남겨 뒀지만 그냥은 안 돌아간다 — 맨 위 주석에 되살릴 코드가 그대로 적혀 있고,
`setLegacyThreat`이 없으면 그 안내를 찍고 종료한다.

### 15) 편성 규칙 실험 — 탱커 수 고정

`scripts/balanceSim.ts`에 5번째 인자(탱커 할당량)를 추가했다. `free`(기본)면 10종 균등 추첨이라
탱커가 0~5명 섞이고 기대값이 1.5명이다. 숫자를 주면 그만큼 탱커에서, 나머지는 비탱커 7종에서 뽑고
**편성 순서를 섞는다**(안 섞으면 탱커가 항상 같은 자리에서 시작해 "탱커 위치 고정"을 재게 된다).

탱커 1명 고정(hard 300판, 옛 위협 모델 기준) 결과 전체 편차가 자유 편성 15.9%p → **13.2%p**로 오히려
좁아졌다. 탱커 3종의 반응이 정반대라는 게 핵심 발견이다:

| 기물 | 0명 | 1명 | 2명 |
|---|---|---|---|
| 방어 강화형 | — | **52.4%** | 48.4% |
| 돌진·기동형 | — | 53.4% | **56.9%** |
| 방벽·제어형 | — | 44.4% | 44.3% |

방어 강화형은 **혼자일 때** 값어치가 나오고(자기가 유일한 전선), 돌진·기동형은 **짝이 있을 때**
값어치가 나온다(파고드는 동안 뒤를 받쳐 줄 몸이 필요). 방벽·제어형만 탱커 수에 무반응(44.4/44.3)인데,
편성을 어떻게 짜도 44%대라는 건 문제가 조합이 아니라 스탯·기술 자체에 있다는 뜻이다.

탱커를 줄이면 판이 늘어진다(평균 16.7→18.8턴, 무승부 1→6판). 무승부 판의 최고 점수를 보면 원인이
갈리는데 탱커 0명은 4.2점(점령지가 계속 경합 0점), 2명은 9.0점(그냥 느린 판)이다. 점령지 경합을 깨고
버티는 역할이 탱커라서, 탱커를 줄이면 점수가 아예 안 쌓이는 교착이 늘어난다 — 교착 규칙이 미정인
현 상태에서는 위험 요소다.

**유보**: 네 번의 실행 모두 p1이 이겼다(151/154/163/153 대 143/141/135/146). 시드를 공유해 돌린 거라
독립 표본이 아니어서 단정할 수 없지만, 배치나 선공권에 p1 편향이 있을 가능성이 있어 별도 시드로
확인해 볼 값어치가 있다.

### 16) 탱커 1명 고정 — 측정용 인자를 실제 편성 규칙으로

시뮬레이터에만 있던 탱커 할당량(15번 항목)을 게임의 편성 규칙으로 올렸다. 시작 화면에서 맵 아래
"편성 규칙" 칸으로 **자유 편성 / 탱커 1명 고정**을 고르고, 로컬·AI·온라인 세 모드에 모두 적용된다.

규칙은 `src/data/rosterRules.ts` **한 곳**에만 적었다. 드래프트 화면(버튼 잠금·확정 조건), 스토어
(`togglePick`·`confirmDraft`), AI 편성, 밸런스 시뮬레이터가 전부 이 파일을 부른다 — 각자 조건을 따로
쓰면 "사람은 못 고르는 편성을 AI만 내는" 식으로 조용히 갈라진다. 실제로 AI 조합 표(`DRAFT_PRESETS`)를
규칙별로 나눠야 했고, 탱커 1명용 어려움 조합은 이 규칙에서 상위권이던 기물로 새로 짰다
(돌진·기동형 + 측면 교란형 + 시간 역행형 + 충전 사격형 + 범위 회복형).

주의해서 지킨 것 세 가지:

- **추첨 함수를 시뮬레이터와 공유하되 난수 소비량까지 같게** 뒀다. 옛 구현과 5가지 할당량 × 2000판을
  맞대 편성과 rng 소비가 완전히 일치함을 확인했다 — 안 그러면 `.balanceSnapshots.json`에 쌓아 둔
  기준선이 전부 비교 불가가 된다.
- **검사는 담을 때만 한다.** 빼는 건 언제나 허용해야 규칙에 걸린 편성을 되돌릴 수 있다. 또 "탱커 자리
  하나가 남았으면 비탱커 버튼을 잠근다"까지 봐야 5기물을 다 채운 뒤 퇴짜 맞는 일이 없다.
- **스토어가 최종 관문**이다. 온라인 게스트는 화면이 아니라 원격 호출(`togglePick`/`confirmDraft`)로
  들어오므로 UI만 잠그면 규칙을 우회할 수 있다. 규칙 자체도 맵과 같이 스냅샷에 실어 호스트 것을 따른다.

### 17) 최소 클릭 — 판을 시작하고 한 턴을 끝내는 데 드는 클릭 줄이기

"최소한의 클릭으로 게임을 즐길 수 있게" 요청. 기능을 빼는 게 아니라 **같은 결과를 더 적은 클릭으로**
얻는 경로를 각 단계마다 하나씩 추가했다. 손으로 하던 길은 전부 그대로 남아 있다.

| 단계 | 이전 | 이후 |
|---|---|---|
| 메뉴 → 전투 | 모드 선택 + 시작 + 드래프트 5픽 + 확정 + 배치 10클릭 + 시작 (약 19) | **1** (「클릭 한 번으로 시작」) |
| 드래프트(한 진영) | 5 | **1** (「추천 편성」) |
| 배치(한 진영) | 10(기물 선택 + 칸 지정 ×5) | **1** (「자동 배치」) |
| 매 턴 | 11(기물 선택 + 칸 지정 ×5, + 해결) | **5**(칸만 지정, 해결은 Space) |

네 가지를 넣었다.

1. **빠른 시작**(`quickStart`) — 추천 편성 + 자동 배치로 AI전을 곧장 만든다. 화면만 건너뛰는 게 아니라
   `draftPicks`·`placementPositions`까지 채워 두어야 「새 게임」이 같은 자리에서 다시 출발한다.
2. **추천 편성**(`autoFillDraft`) / **자동 배치**(`autoPlace`) — 사람용 조합을 따로 적지 않고
   `aiDraftPicks('hard')`·`aiPlacement`를 그대로 부른다. 밸런스 측정에서 상위권이던 편성이고, 여기에
   또 적으면 같은 근거가 두 곳으로 갈려 한쪽만 낡는다. 편성 규칙(16절)도 그 표가 이미 지킨다.
   자동 배치는 **손으로 놓은 기물은 건드리지 않고 빈 자리만** 채우며, 같은 칸 중복을 `Set`으로 막는다.
3. **자동 넘기기** — 한 기물의 계획이 끝나면 다음 미계획 기물로 선택이 저절로 옮겨간다(순환 탐색,
   다 끝나면 선택 해제). 매 턴 5번씩 눌러야 했던 "기물 선택" 클릭이 통째로 사라진다.
4. **적 칸 클릭 = 공격**, **Space/Enter = 공개 및 해결**. 클릭 모드 토글은 남겼다 — 빈 칸을 조준하려면
   여전히 필요하다.

**자동 넘기기의 유일한 위험은 "아직 안 끝났는데 넘어가는 것"이다.** dealer2(시간 역행)처럼 이동을 여러
번 얻은 기물은 한 번 찍어도 계획이 안 끝나므로, `hasPendingMoveSegment()`(`Planning/actionGeometry.ts`)가
"용량이 있는데 아직 0칸인 이동 구간"이 남았는지 보고 남아 있으면 넘기지 않는다. 기본 이동이 막힌
기술이동 계획(공격 후 이동)에서는 0번 구간을 세지 않는다. 그래도 넘어가서 손해 보는 클릭 1번보다
매 턴 아껴지는 클릭이 훨씬 많다는 판단으로, 애매하면 넘기는 쪽을 택했다.

**tank2 돌진 예외**: "적이 있는 칸 = 공격"이 기본이지만 돌진은 적을 밟고 지나가는 기술이라
`isDashPlanning()`이면 그 칸을 이동 목적지로 해석한다. 이 예외가 없으면 돌진이 아예 못 쓰인다.

**온라인**: `autoFillDraft`·`autoPlace`는 사람이 부르는 변경이므로 `RemoteAction` 유니온과
`applyRemoteAction`에 함께 넣고 `forwardIfGuest`로 감쌌다. 게스트가 자기 스토어를 직접 고치면
방장 권위가 깨진다. `quickStart`는 메뉴 전용이라 전달하지 않는다.

**검증**: `test/ui/minimalClicks.test.ts` 6건 — 빠른 시작이 드래프트·배치를 거친 것과 같은 상태를 만드는지,
모든 편성 규칙에서 규칙을 지키는지, 자동 배치가 손으로 놓은 기물을 보존하고 겹치지 않는지,
`hasPendingMoveSegment`가 dealer2의 남은 구간을 잡아내는지. 브라우저에서 실제로 1클릭 시작 →
매 턴 5클릭 + Space 경로를 끝까지 재현해 확인했다.

### 18) 도움말 화면 — 규칙과 기물 사정권을 그림으로

"처음 보는 사용자를 위해 룰과 기물이 어떻게 움직이는지 시각화" 요청. 화면(`components/Guide/`)은 다섯 절이다:
①무엇을 하면 이기는가(실제 맵 그림) ②한 턴의 5단계 ③점령 점수 예시 ④기물 10종 사정권 ⑤조작.

**설계의 전부는 "그리지 않고 계산한다"이다.** 도움말은 구조적으로 낡는 문서다 — 지금까지의 작업 이력만
봐도 기물 스탯은 6절·13절·14절에서, 맵 크기는 9절에서, 점령 규칙은 CAPTURE_MARGIN 도입 때 바뀌었다.
설명을 손으로 적으면 그 순간마다 갈라진다. 그래서 도움말에 손으로 적힌 숫자는 **하나도 없다**.

| 도움말이 보여주는 것 | 어디서 나오는가 |
|---|---|
| 기물별 이동/공격 가능 칸 | 판이 쓰는 `computeMoveOptions`·`computeAttackOptions`를 빈 보드에 그대로 호출 |
| 회복 사정권 | 같은 `isWithinSkillRange`로 전 칸을 훑음 |
| 체력·공격력·사거리·쿨타임 문구 | `unitTypes.ts`의 값에서 조립(`attackRangeLabel` 등) |
| 점령 예시의 판정 캡션 | `captureWinner(counts)`의 반환값 |
| 승리 점수·부활 턴 수 | `constants.ts` |

손으로 적은 건 숫자가 없는 기술 설명문(`SKILL_BLURB`)뿐이다. 수치는 옆에 `skillGateLabel`이 붙인다.

이를 위해 **점령 규칙을 `engine/capture.ts`로 분리**했다(`resolveEndOfTurn`에서 추출, 동작 불변 —
기존 테스트 전건 통과로 확인). 규칙 조문이 엔진과 설명 두 곳에 적히면 반드시 한쪽이 낡는데,
`data/rosterRules.ts`에서 이미 쓰던 방식이다.

**그림판은 `Board.tsx`를 재사용하지 않고 `MiniBoard.tsx`를 새로 만들었다.** 실제 보드는 계획·선택·
미리보기 같은 대전 상태를 요구하지만 설명 그림은 상태가 없어야 한다. 필요한 건 "칸을 칠하고 도형을
얹는다"뿐이라 상태 없는 SVG 하나가 맞다. 역할 실루엣(탱커=사각/딜러=삼각/지원=원)만 판과 똑같이 쓴다 —
도움말에서 배운 모양이 대전 화면에서 그대로 보여야 하기 때문.

**격자 크기는 전 기물 공통(반지름 6, 13×13)이다.** 기물마다 딱 맞게 줄이면 사거리 2와 6이 같은 크기로
보여 "누가 더 멀리 치는지"라는 가장 중요한 정보가 사라진다.

**도움말은 단계(stage)가 아니라 오버레이**이고, 열림 상태는 스토어가 아닌 `App`의 `useState`에 둔다.
온라인 대전은 방장 스냅샷으로 스토어를 통째로 갈아끼우므로, 스토어에 넣으면 상대 턴마다 도움말이
제멋대로 열리고 닫힌다. 대신 열려 있는 동안 `GameScreen`의 키 단축키를 끈다(`shortcutsOff`) — 안 그러면
도움말 위에서 누른 Space가 뒤에 깔린 판의 턴을 넘겨 버린다.

**검증**: `test/ui/guide.test.ts` 9건 — 모든 기물이 이동 칸을 갖는지, 격자가 최장 사거리를 담는지,
tank1(직선)과 dealer3(대각선 포함)의 모양 차이, support2의 "공격 불가", support3 동전 앞/뒷면 이동 구분,
`captureWinner` 3케이스. 테스트 중 tank3 범위공격 칸이 12가 아닌 8인 걸 발견했는데 코드가 아니라 내
예상이 틀린 것이었다(네 방향 전방밴드가 모서리를 공유). 다만 이걸 보고 "빨간 점 = 한 방에 맞는 범위"로
오해할 여지가 드러나 도움말에 사정권이라는 주석을 명시했다.

### 19) 판 길이 재조정 — WIN_SCORE 7 → 15 ("평균 판당 턴수를 35~40턴 사이로")

7은 17절에서 CAPTURE_MARGIN(인원 차 2) 도입 뒤 늘어난 판 길이를 되돌리려고 고른 값이었다.
이번엔 반대 방향 요청이라 같은 손잡이를 다시 돌렸다.

**측정(300판, 자유 편성·정원 맵·hard AI 미러전, `scripts/balanceSim.ts`)**: 시드가 고정돼 있어
같은 설정을 다시 돌려도 값이 그대로 나온다.

| WIN_SCORE | 평균 턴수 | 총 데스/판(양측 합산) | 전체편차 | 무승부율 |
|---|---|---|---|---|
| 7(기존) | 24.0턴 | ~4.1회 | 19.8%p | 4.0% |
| 10 | 29.6턴 | ~5.0회 | 19.4%p | 5.7% |
| 14 | 35.2턴 | ~5.7회 | 19.0%p | 8.0% |
| **15(채택)** | **36.8턴** | ~5.9회 | 18.4%p | 9.0% |

점수-턴수 관계는 거의 선형이다(1점당 약 +1.9턴). 15는 목표 구간(35~40턴) 중앙에 걸리고 14보다
전체편차가 근소하게 낮아 이 값을 택했다. 대가는 무승부율 증가(4.0 → 9.0%) — 실제 게임에는 턴
제한이 없으므로 이건 시뮬레이터의 `MAX_TURNS`(80) 상한 안에 못 끝나는 판이 늘었다는 뜻이고,
실전 판 길이에는 영향이 없다.

**기물 서열은 WIN_SCORE와 무관하게 그대로였다** — 측면 교란형이 4개 값 전부에서 1위, 방벽·제어형
(방벽이 자연 만료로만 사라지는 tank3)이 판당사망 0.02로 전부에서 사실상 안 죽는 이상치로 남았다.
판 길이를 늘려도 순위 자체가 바뀌지 않는다는 건, 지금 편차가 판 길이가 아니라 기물 값어치 자체의
문제라는 뜻이다 — 다음 밸런스 조정은 이 순위표를 그대로 근거로 써도 된다.

README·PROGRESS의 "7점 승리"·"10점 승리"(핵심 규칙 현황·검증 절, CAPTURE_MARGIN 도입 이전부터
남아있던 진짜 오래된 스테일 표기)를 모두 15로 맞췄다.

### 20) support3 병목 규명 실험 3종 — 두 개 기각, 하나 채택(목적과 다른 이유로)

측정에서 확률·포탑형(support3)이 계속 최하위(44.9%)라 원인을 찾는 실험을 셋 돌렸다. 전부 1000판·
시드 짝지음·정원 맵·hard AI 미러전이고, 각각 `main`에서 딴 독립 브랜치에서 쟀다 — 서로 얹으면
효과가 섞여 각 변경의 값어치를 알 수 없다.

| 실험 | support3 | 전체편차 | 결론 |
|---|---|---|---|
| 기준선 | 44.9% | 16.1%p | — |
| ① 동전을 계획 전에 확정 | 42.0% (▼2.9) | 18.5%p | 기각 |
| ② 포탑 체력 1 → 8 | 45.3% (▲0.4) | **14.8%p** | **채택**(다른 이유로) |
| ③ 부활 2 → 5턴 | 43.5% (▼1.4) | 15.0%p | 기각 |

**① 동전 타이밍** — support3만 "계획한 대로 되지 않는 것이 기본값"인 구조였다(동전이 해결 단계에서
굴러 계획은 앞면 기준으로 세우고 뒷면이면 잘렸다). 이전 턴 종료에 미리 굴려 상한과 실제값을
일치시켰더니 **오히려 내려갔다**. 이동만 바꾼 변형도 42.1%로 같아 원인은 이동 쪽이었다:
기대 이동거리는 1.5칸으로 동일하지만 "2칸 경로를 짜고 뒷면이면 앞 1칸만 실행"이 **최적 2칸 경로의
첫 걸음을 고르는 공짜 선행 탐색**으로 작동하고 있었다. 낙관적 계획이 버그가 아니라 이득이었다.
단, 시뮬레이터는 AI 대전만 재고 AI는 매 턴 새로 계획하므로 계획이 잘리는 비용을 치르지 않는다 —
사람의 다턴 경로 계획이 무너지는 문제는 이 측정이 잴 수 없는 축이다.

**③ 부활 턴수** — "죽음의 대가가 커지면 회복이 귀해져 지원 3종이 함께 오른다"는 가설이었으나
셋이 갈렸다(support2 ▲1.4 / support1 ▼2.2 / support3 ▼1.4). **회복은 수요와 함께 공급도 줄어든다** —
죽은 아군이 오래 누워 있으면 회복할 대상이 없어 판당 회복량 자체가 감소했다(support1 190.0 → 179.7).
실제로 작동한 메커니즘은 "자주 죽는 공격적 기물이 벌을 받는다"였다(측면 교란형 ▼2.5로 최대 낙폭).

**서로 다른 세 구조 가설이 전부 support3을 못 움직였다.** 남은 미검증 축은 **이동력**이고,
선례가 강하다 — 충전 사격형도 사거리를 5·6으로 올려도 ±0.0~+2.6%p였는데 이동만 2로 올리자
+8.1%p였다(unitTypes.ts dealer3 주석). support3은 평균 이동력 1.5에 공격 사거리 2, 포탑은 앞칸이라
"뭘 하려면 붙어야 하는데 붙을 발이 없는" 같은 형태다.

**②를 채택한 이유는 목적과 다르다.** support3은 못 고쳤지만 전체편차가 측정 이래 최소치(14.8%p)가
됐다. 이 사실은 `unitTypes.ts`의 포탑 주석에도 적어 뒀다 — "support3을 위한 값"으로 오해하고
되돌리면 편차만 다시 벌어진다.

기각한 두 실험은 브랜치(`exp/support3-coin-before-planning`, `exp/respawn-turns`)에 기각 사유까지
커밋해 남겼다. 같은 가설을 다시 세우기 전에 그 커밋 메시지를 먼저 읽으면 된다.

### 21) 부활 2 → 3턴, 힐팩 재생성 3 → 5턴 (측정과 반대로 간 결정)

**측정만 보면 손해인 변경을 사용자 판단으로 채택한 사례다.** 1000판 시드 짝지음 정원 맵에서
전체편차가 14.8 → 16.5%p로 벌어졌고, 하필 양쪽 이상치가 둘 다 잘못된 방향으로 움직였다 —
1위 측면 교란형이 60.1 → 60.7%로 더 세지고, 10위 확률·포탑형이 45.3 → 44.2%로 더 약해졌다.
20절의 5턴 실험도 방향이 같았으니 두 번 재현된 경향이다.

**그럼에도 채택한 근거는 시뮬레이터가 잴 수 없는 축이 있다는 것이다.** balanceSim은 AI 미러전만
재는데, AI는 매 턴 계획을 새로 세우고 죽음을 두려워하지 않으므로 "죽으면 몇 턴 쉬는가"의 체감
비용을 치르지 않는다. 사람에게는 그 대기 시간이 판의 리듬 그 자체다. 편차 1.7%p를 체감과
맞바꾼 것이고, **이런 트레이드오프에서는 숫자가 결정권을 갖지 않는다**는 판단이다.

**힐팩 5턴은 부등호를 지키기 위한 동반 변경이다.** `HEAL_PACK_RESPAWN_TURNS`에는 "부활보다
길어야 한다"는 근거가 적혀 있다 — 힐팩 재생성이 부활보다 빠르면 "죽지 않고 힐팩만 순회하는"
운영이 언제나 옳아져 교전을 회피하는 판이 되기 때문이다. 부활이 3이 되며 두 값이 같아져 그
부등호가 깨졌다(길다 → 같다). 4면 당장은 충분하지만 아슬아슬하게만 성립하면 다음에 부활을
또 올릴 때 다시 깨지므로 5로 여유를 뒀다.

**힐팩 맵은 따로 쟀다.** 정원 맵에는 힐팩이 없어 기본 측정으로는 이 변경이 아예 안 잡힌다.
`scripts/maps/packs-at-gates.txt`에서 `main`과 변경본을 각각 1000판씩 돌려 비교한 결과,
편차는 29.5 → 30.8%p로 정원과 같은 방향이었으나 **무승부가 99 → 76판으로 줄었다** —
부활과 힐팩이 함께 느려지며 교착이 줄어든 것으로 보인다. 체감을 위해 택한 변경이 판을 더
분명하게 끝내기도 한다는 뜻이라, 이 맵에서는 손해만 있는 게 아니다.

**부수 발견: 힐팩 맵의 밸런스가 정원 맵과 근본적으로 다르다.** 편차가 정원(14.8%p)의 두 배인
29.5%p이고, 특히 방벽·제어형이 정원 50.2% → 이 맵 38.2%로 무너진다(판당피해 54.9 → 12.6,
점령체류 22.1 → 3.4). 맵 하나가 기물 하나를 무력화할 수 있다는 뜻이고, 이 맵 자체의 문제로
따로 볼 값이다 — 상수 조정으로 고칠 수 있는 종류가 아니다.

### 22) 측면 교란형 하향 — 패시브 +7 → +5, 전체편차 측정 이래 최소

측면 교란형(dealer4)은 **측정 이래 모든 설정에서 1위**였다 — WIN_SCORE 7·10·14·15, 편성 규칙 2종,
20~21절의 실험 4종 어디서도 흔들리지 않았고 2위와 7%p 이상 벌어져 있었다.

**원인은 표기 공격력이 아니라 패시브였다.** `dealer4Flank.ts` 1000판 측정:

| | 보너스 7(before) | 보너스 5(after) |
|---|---|---|
| 발동률 | 57.0% | 59.3% |
| 전체 피해 중 패시브 몫 | **43.4%** | 36.3% |
| **실효 공격력** | **9.18** | 8.18 |
| 승점률 | 60.7% | 58.9% |
| **전체편차** | 16.5%p | **14.4%p** |

표기 공격력 5는 10종 중 7위인데 실효 9.18은 dealer1(9)과 같은 상위권이었다 — **스탯 시트가 실제를
반영하지 못하는 상태**였고, 이게 "약한 공격력을 조건부 보너스로 보완한다"는 설계 의도와 실제 성능이
갈라진 지점이다.

**깎인 화력이 특정 기물이 아니라 중하위권에 골고루 갔다**(돌진·기동형 ▲2.0, 시간 역행형 ▲1.1,
지원 2종 각 ▲1.0). 1위와 2위 격차도 7.6 → 4.1%p로 절반이 됐다.

**되먹임이 또 확인됐다**: 승점 하락폭(-1.8%p)이 화력 삭감분(실효 -1.0)보다 작다. 한 방이 약해지자
AI 위협 모델이 이 기물을 덜 피해 명중이 9.3 → 10.0회로 **늘었기** 때문이다. dealer3에서 공격력을
16 → 12로 내렸는데 판당피해가 올랐던 것과 같은 현상이다(unitTypes.ts dealer3 attack 주석).

**남은 두 가지**:

1. **조건은 손대지 않았다.** 점령지 **밖** 발동률(66.5%)이 **안**(54.3%)보다 높다 — 원래 의심이었던
   "점령지에 적이 뭉쳐서 조건이 저절로 충족된다"가 아니라 조건 자체가 헐거운 것이다
   (`hasAdjacentAlly`는 대상 상하좌우 중 하나라도 아군이면 참). "측면을 파고든 보상"이라는
   이름값을 못 하는 구조는 그대로이고, 조건을 조이는 안(대각 인접 요구, 인접 아군 2명 이상)은
   미검증으로 남아 있다.
2. **이 숫자는 자리 교체를 한 번도 안 쓴 값이다.** AI가 `dealer4_swap`을 계획하지 않는다
   (aiPlayer.ts에 해당 id가 0회 등장). 사람이 쓰면 58.9%보다 위다.

**하위권은 이 조정으로 안 움직였다**(확률·포탑형 ▲0.2, 방어 강화형 ▼0.8). 편차가 준 건 위를
눌러서지 아래를 올려서가 아니라는 뜻이라, 하위권 문제는 20절의 미검증 축(이동력)으로 남아 있다.

### 23) 확률·포탑형 이동·사거리 상향 — 최하위 탈출, 편차 측정 이래 최소

20절에서 구조 가설 셋(동전 타이밍·포탑 체력·부활 턴수)이 전부 실패한 뒤 **남겨 둔 유일한 축이
이동**이었다. 앞면을 이동2·사거리2 → **이동3·사거리3**으로 올렸다(뒷면 1/2는 그대로, 공격력 4/6도
그대로). 1000판 시드 짝지음:

| | before | after |
|---|---|---|
| 승점률 | 44.5% (10위) | **48.5% (6위)** |
| 판당피해 | 15.6 | 20.3 |
| 점령체류 | 13.7 | 16.4 |
| 판당사망 | 0.60 | 0.74 |
| **전체편차** | 14.4%p | **13.2%p** |

**진단이 맞았다 — "붙어야 일하는데 붙을 발이 없다"였다.** 공격력을 한 자릿수도 안 건드렸는데
판당피해가 30% 올랐다(쏠 기회가 는 것이다). 점령체류도 함께 올랐고, 판당사망이 오른 건 앞으로
나가게 됐다는 대가다. 충전 사격형의 선례(사거리는 안 듣고 이동만 +8.1%p)가 그대로 재현됐다.

**포탑 지표는 오히려 내려갔다**(생성당 6.8 → 6.5, 총량 202,845 → 190,025). support3이 앞으로
나가면서 포탑을 두고 떠나기 때문으로 보인다. 승점은 올랐는데 포탑 성능은 내려간 셈이라, 이제 이
기물은 "포탑형"보다 **확률형 전투 지원**에 가깝다 — 이름과 실제가 갈라지기 시작한 지점이다.

#### 엔진 구조: 사거리가 턴마다 갈리는 첫 기물

`attackRangeFor()`는 방향(직선/대각)만 보고 `shape`를 받으므로 동전 상태를 알 방법이 없다. 그래서
**shape를 건네기 전에 갈아 끼우는** 방식을 택했다 — `unitStats.ts`에 `resolvedAttackShape`(해결용,
실제 동전)와 `plannedAttackShape`(계획·UI·AI용, 앞면 상한)를 추가하고 세 호출부(attacks.ts,
actionGeometry.ts, aiPlayer.ts)가 그걸 거치게 했다. 이동력·공격력이 이미 쓰던 패턴과 같고,
"방향별 사거리는 attackRangeFor 한 곳"이라는 기존 불변식도 깨지지 않는다.

**작업 중 버그를 하나 잡았다.** `threatAt`에서 사거리를 `plannedAttackShape(unit)`으로 넣었는데
이 함수는 **적의** 위협을 재는 곳이라 `enemy`여야 했다. 바로 위 주석에 14절에서 이동력으로 똑같은
실수를 했다는 기록이 남아 있어 발견했다 — 그대로 뒀으면 AI가 잘못된 사거리로 위험을 판단했다.
**같은 함수에서 같은 종류의 실수가 두 번 났다**는 건 이 함수의 `unit`/`enemy` 구분이 구조적으로
헷갈린다는 뜻이다.

#### 부수 효과

- **측면 교란형이 다시 올랐다**(58.9 → 59.8%). support3이 앞으로 나오며 패시브 조건(인접한 적)을
  더 자주 만들어 주는 것으로 보인다. 1위−2위 격차도 4.1 → 6.0%p로 벌어졌다.
- **범위 회복형이 새 최하위**(46.6%)다. 하단을 잡는 기물이 바뀌었다.
- 무승부가 7.0 → 8.7%로 늘었다.

### 24) 기물 UI — 볼 수 없던 정보를 만들고, 같은 역할끼리 구별되게

밸런스 조정을 여러 번 하고 나니 **화면이 실제를 반영하지 못하는 지점**이 드러났다. 세 가지를 고쳤다.

#### 1) 아예 볼 수 없던 정보

- **탄창(장거리 화력형)**: "2발 쏘고 1턴 휴식"이 이 기물의 유일한 운용 축인데 `attackShots`를 읽는
  컴포넌트가 **하나도 없었다**. 잔탄이 한 값이 아니라 `charges`(쏜 횟수)와 `cooldowns`(휴식)에
  나뉘어 있고 둘의 관계가 직관적이지 않아서, UI가 직접 계산하면 틀리기 쉽다 — `unitStats.ts`에
  `ammoState()`를 두고 테스트 4건으로 잠갔다. 실제로 이 함수를 처음 쓸 때 쿨타임의 `+1`(같은 턴
  종료에 깎이는 일시값)을 보정하려다 한 턴을 덜 세는 실수를 했고, 테스트가 그걸 잡았다.
- **측면 보너스(측면 교란형)**: 발동률 59.3%에 전체 피해의 36.3%를 차지하는데(표기 공격력 5,
  실효 8.18) 화면에 아무 단서도 없었다. 조준 중인 사거리 안에서 조건을 채운 적에게 `+5`와 주황
  점선 테두리를 띄운다. 조건·수치는 `engine/flankBonus.ts`로 빼서 **해결 단계가 부르는 바로 그
  함수**를 화면도 부른다(점령 규칙을 `capture.ts`로 뺀 것과 같은 이유) — 화면에 뜬 +N과 실제
  피해가 갈라질 수 없다. 실제 대전에서 이 조건을 만들려면 특정 편성이 뽑히고 적 둘이 붙어 서야
  해서 브라우저로 재현하기 어렵다. 그래서 렌더 테스트 3건으로 잠갔다.

#### 2) 같은 역할끼리 구별되지 않던 문제

판 위에서 기물을 가르는 수단은 **역할 실루엣 3종 + 라벨**뿐인데, 라벨이 `T1`/`D4`라 종류를
구별해 주지도, 무엇을 하는 기물인지 알려주지도 못했다. 데이터에 `shortLabel`(한 글자)을 두고
방·돌·벽·저·역·충·측·범·구·탑으로 갈랐다.

**이름에서 유도하지 않고 손으로 적는다** — "방어 강화형"과 "방벽·제어형", "장거리 화력형"과
"장거리 회복·구속형"처럼 앞 글자가 겹치는 짝이 있어 자동 생성하면 구별이 안 된다. 구별이 목적인
값이라 10종+포탑이 전부 달라야 하고, `test/data/unitTypes.test.ts`가 그걸 잠근다.

글자 크기는 칸 크기에 비례시켰다(기존 9px 고정 → `cellSize * 0.3`). 라틴 문자는 9px로도 읽혔지만
한글은 뭉개진다 — 구별이 목적인 글자가 안 읽히면 수단이 통째로 사라진다. 삼각형(딜러)은 중심
높이에서 폭이 절반뿐이라 글자가 빗변 밖으로 나가므로 넓은 밑변 쪽으로 조금 내려 찍는다.
도움말 기물 카드에도 같은 글자를 찍어 "도움말에서 외운 글자가 판에서 그대로 보이게" 했다.

#### 3) 화면이 스탯을 실제보다 낮게 말하던 문제

확률·포탑형은 이동·사거리는 "1 또는 3칸(동전)"으로 표기되는데 **공격력만 뒷면 값(4)으로 고정
표시**되고 있었다(실제 4~6). `attackPowerLabel()`을 추가해 세 스탯이 모두 동전 범위로 나온다.

자원 표시도 성격별로 갈랐다 — 탄창은 상태(켜짐/꺼짐)가 아니라 **소모되는 자원**이라 남아 있을 때는
중립색, 다 쓰고 쉬는 중일 때만 경고색으로 바뀐다.

**남은 UI 과제**(브리핑에서 정리했으나 이번에 손대지 않음): 공격 모드 토글의 *대가*(이동 불가)를
켜진 동안 드러내기, 시간 역행형의 복귀 지점 강조, 범위 회복형의 자기중심 반경 2 표시,
장거리 회복·구속형의 공격 불가를 조작 단계에서 드러내기.

## 남아있는 알려진 이슈 / 미정 사항

- 스테일메이트(교착) 규칙: 기획서 자체가 미정 — 구현하지 않음(TODO 주석만 존재).
- **AI가 `dealer4_swap`(대각선 3칸 아군과 자리교체)을 계획하지 않는다.** 기술은 엔진에 정상 구현돼
  있고 사람이 쓰면 동작하지만 AI 후보 생성에 빠져 있어, AI 대전에서 dealer4가 이 기술을 영영 안 쓴다.
  밸런스 수치(dealer4 승점 56~60%)는 이 기술이 **없는 셈 친** 값이다.
- 기획서에 수치가 없어 기본값을 채택한 항목들(정원 맵 정확한 그리드/장애물, support1·support2 쿨타임,
  보호막 부여량 공식, 보호막·방벽 동시 적용 우선순위, 포탑 배치 방향 기준, 사망 시 쿨타임/충전 초기화
  여부, 부활 시 인접 칸 탐색 순서, 동률 우선순위 타이브레이크 방식)은
  `/Users/jungtaemin/.claude/plans/3-joyful-dolphin.md`의 "남은 판단 필요 항목" 절 및 관련 소스 코드
  주석에 채택한 기본값과 함께 정리되어 있음.
